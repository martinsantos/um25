// @ts-nocheck
// Configuración de Jest para Astro con soporte para módulos ES

module.exports = {
  // Configuración básica
  testEnvironment: 'jsdom',
  testMatch: [
    '**/__tests__/**/*.[jt]s?(x)',
    '**/?(*.)+(spec|test).[jt]s?(x)'
  ],
  testPathIgnorePatterns: [
    '/node_modules/',
    '/.astro/',
    '/.netlify/',
    '/dist/'
  ],
  
  // Configuración de módulos
  moduleFileExtensions: ['js', 'jsx', 'ts', 'tsx', 'json', 'node'],
  moduleNameMapper: {
    // Alias de rutas
    '^@/(.*)$': '<rootDir>/src/$1',
    '^@components/(.*)$': '<rootDir>/src/components/$1',
    '^@pages/(.*)$': '<rootDir>/src/pages/$1',
    '^@utils/(.*)$': '<rootDir>/src/utils/$1',
    // Mocks para archivos estáticos
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
    '\\.(jpg|jpeg|png|gif|eot|otf|webp|svg|ttf|woff|woff2|mp4|webm|wav|mp3|m4a|aac|oga)$':
      '<rootDir>/__mocks__/fileMock.js'
  },
  
  // Configuración de transformación
  transform: {
    '^.+\\.(js|jsx|ts|tsx)$': 'babel-jest',
    '^.+\\.astro$': './jest-astro-transformer.cjs',
    '^.+\\.(css|styl|less|sass|scss|png|jpg|ttf|woff|woff2)$': 'jest-transform-stub'
  },
  transformIgnorePatterns: [
    '/node_modules/(?!(astro|@astrojs)/)',
    'node_modules/(?!(jest-)?@?react' // Ignorar node_modules excepto react
  ],
  
  // Configuración para setup
  setupFiles: [
    '<rootDir>/jest.setup.js' // Solo el setup personalizado aquí
  ],
  setupFilesAfterEnv: [
    '@testing-library/jest-dom', // Configuración de testing-library después del entorno
    'jest-expect-message' // Mejores mensajes de error
  ],
  
  // Otras configuraciones
  verbose: true,
  bail: false, // Desactivado para ver todos los fallos
  testEnvironmentOptions: {
    url: 'http://localhost',
    customExportConditions: ['node', 'node-addons']
  },
  
  // Configuración para TypeScript
  preset: 'ts-jest',
  globals: {
    'ts-jest': {
      tsconfig: 'tsconfig.jest.json',
      isolatedModules: true
    }
  },
  
  // Configuración de cobertura
  collectCoverage: true,
  collectCoverageFrom: [
    'src/**/*.{js,jsx,ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.stories.{js,jsx,ts,tsx}',
    '!src/main.jsx',
    '!src/main.tsx',
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov'],
  
  // Configuración para manejar módulos ES
  extensionsToTreatAsEsm: ['.ts', '.tsx'],
  moduleDirectories: ['node_modules', 'src']
};
