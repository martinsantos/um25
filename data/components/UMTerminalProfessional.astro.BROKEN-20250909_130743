---
// UMTerminalEnhanced.astro - Enhanced terminal with groundbreaking visual effects and advanced UX
---

<div id="um-terminal-enhanced-container">
  <div id="um-terminal-enhanced" class="um-terminal-enhanced">
    <!-- Terminal Header -->
    <div class="um-terminal-header">
    <div class="um-terminal-controls">
        <button class="um-control um-minimize" data-action="minimize" aria-label="Minimizar terminal">
          <span class="control-icon">−</span>
        </button>
        <button class="um-control um-maximize" data-action="fullscreen" aria-label="Pantalla completa">
          <span class="control-icon">⬜</span>
        </button>
        <button class="um-control um-close" data-action="close" aria-label="Cerrar terminal">
          <span class="control-icon">✕</span>
        </button>
      </div>
      <div class="um-terminal-title">ULTIMA MILLA CLI v22.0 - Mendoza, Argentina</div>
      <div class="um-terminal-theme-selector">
        <select id="theme-selector" title="Seleccionar tema" aria-label="Selector de tema visual">
          <option value="default">Terminal</option>
          <option value="matrix">Matrix</option>
          <option value="retro">Retro</option>
          <option value="neon">Neon</option>
          <option value="corporate">Corporativo</option>
        </select>
      </div>
      <div class="um-terminal-status">
        <div class="status-indicator" id="connection-status" aria-label="Estado de conexión"></div>
      </div>
    </div>
    
    <!-- Terminal Body -->
    <div class="um-terminal-body" id="terminal-output">
      <div class="welcome-message">
        <pre id="welcome-ascii" class="ascii-art"></pre>
        <div class="welcome-info">
        <div class="welcome-line typing-effect" data-delay="0">
            <span class="icon">🚀</span> Bienvenido a <strong>ULTIMA MILLA CLI</strong> - Terminal interactivo empresarial
          </div>
          <div class="welcome-line typing-effect" data-delay="800">
            <span class="icon">📊</span> Explora <strong>22 años</strong> de historia, <strong>469+ proyectos</strong> y <strong>150+ clientes</strong>
          </div>
          <div class="welcome-line typing-effect" data-delay="1600">
            <span class="icon">💡</span> Escribe <code>help</code> para ver comandos disponibles
          </div>
          <div class="welcome-line typing-effect" data-delay="2400">
            <span class="icon">⚡</span> Prueba <code>sudo ultimamilla.py --demo</code> para una demostración completa
          </div>
          <div class="welcome-line typing-effect" data-delay="3200">
            <span class="icon">📞</span> Comando <code>contacto</code> para enviar mensajes reales
          </div>
        </div>
      </div>
    </div>
    
    <!-- Terminal Input Line -->
    <div class="um-terminal-input-line">
      <div class="input-container">
        <span class="um-prompt" id="terminal-prompt">visitante@ultimamilla:~$ </span>
        <input 
          type="text" 
          id="terminal-input" 
          class="um-input" 
          autocomplete="off"
          spellcheck="false"
          placeholder="Escribe un comando..."
          aria-label="Terminal de comandos"
        />
        <div class="cursor-blink" id="terminal-cursor"></div>
      </div>
      <div class="input-suggestions enhanced-suggestions" id="input-suggestions"></div>
    </div>
    
    <!-- Loading Overlay with Enhanced Progress -->
    <div class="loading-overlay" id="loading-overlay">
      <div class="loading-spinner matrix-spinner"></div>
      <div class="loading-text">Procesando comando...</div>
      <div class="loading-progress">
        <div class="progress-bar" id="progress-bar"></div>
      </div>
    </div>
    
    <!-- Matrix Rain Effect Canvas -->
    <canvas id="matrix-canvas" class="matrix-rain" style="display: none;"></canvas>
  </div>
</div>

<!-- Scripts del terminal integrados (sin scripts externos) -->

<style>
  /* Enhanced Terminal Container */
  #um-terminal-enhanced-container {
    width: 100%;
    max-width: none;
    margin: 0 auto;
    padding: 0;
    position: relative;
    z-index: 10;
  }

  .um-terminal-enhanced {
    /* Responsive sizing - balanced with background visibility */
    width: 85vw;
    max-width: 1200px;
    height: 70vh;
    min-height: 500px;
    max-height: 800px;
    
    /* Enhanced visual design */
    background: linear-gradient(145deg, #0a0e1a 0%, #1a1f2e 50%, #0d1117 100%);
    border: 2px solid transparent;
    background-clip: padding-box;
    border-radius: 16px;
    
    /* Enhanced shadow and glow effects */
    box-shadow: 
      0 25px 50px -12px rgba(0, 0, 0, 0.8),
      0 0 0 1px rgba(255, 255, 255, 0.05),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
    
    /* Font and layout */
    font-family: 'Fira Code', 'JetBrains Mono', 'SF Mono', 'Monaco', 'Cascadia Code', 'Roboto Mono', monospace;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    position: relative;
    margin: 0 auto;
    
    /* Subtle animation */
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Glowing border effect */
  .um-terminal-enhanced::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg, 
      rgba(0, 255, 170, 0.3) 0%,
      rgba(121, 192, 255, 0.3) 25%,
      rgba(255, 107, 107, 0.3) 50%,
      rgba(255, 214, 0, 0.3) 75%,
      rgba(0, 255, 170, 0.3) 100%
    );
    border-radius: 16px;
    z-index: -1;
    animation: borderGlow 4s linear infinite;
  }

  @keyframes borderGlow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  /* Terminal Header */
  .um-terminal-header {
    background: linear-gradient(135deg, #21262d 0%, #30363d 100%);
    padding: 16px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    user-select: none;
    backdrop-filter: blur(10px);
  }

  .um-terminal-controls {
    display: flex;
    gap: 12px;
    align-items: center;
  }

  .um-control {
    width: 32px;
    height: 26px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.1);
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    backdrop-filter: blur(5px);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .um-minimize {
    background: rgba(255, 255, 255, 0.1);
  }

  .um-minimize:hover {
    background: rgba(255, 255, 255, 0.2);
  }

  .um-maximize {
    background: rgba(255, 255, 255, 0.1);
  }

  .um-maximize:hover {
    background: rgba(255, 255, 255, 0.2);
  }

  .um-close {
    background: rgba(255, 255, 255, 0.1);
  }

  .um-close:hover {
    background: #e74c3c;
    border-color: #c0392b;
  }

  .control-icon {
    color: #ffffff;
    font-size: 12px;
    font-weight: bold;
    line-height: 1;
  }

  .um-control:active {
    transform: scale(0.95);
  }

  .um-terminal-title {
    color: #e6edf3;
    font-size: 14px;
    font-weight: 600;
    text-align: center;
    flex: 1;
    margin-left: -80px;
    text-shadow: 0 0 10px rgba(230, 237, 243, 0.3);
  }

  .um-terminal-status {
    display: flex;
    align-items: center;
  }

  .status-indicator {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: linear-gradient(45deg, #00d4aa, #00ff88);
    box-shadow: 0 0 15px rgba(0, 212, 170, 0.5);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.7; transform: scale(0.9); }
  }

  /* Terminal Body */
  .um-terminal-body {
    flex: 1;
    padding: 24px;
    overflow-y: auto;
    color: #e6edf3;
    font-size: 16px;
    line-height: 1.6;
    background: rgba(13, 17, 23, 0.4);
    backdrop-filter: blur(5px);
    position: relative;
  }

  /* Custom scrollbar */
  .um-terminal-body::-webkit-scrollbar {
    width: 8px;
  }

  .um-terminal-body::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 4px;
  }

  .um-terminal-body::-webkit-scrollbar-thumb {
    background: linear-gradient(135deg, #00d4aa, #79c0ff);
    border-radius: 4px;
  }

  .um-terminal-body::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(135deg, #00ff88, #a0d0ff);
  }

  /* Welcome Message */
  .welcome-message {
    margin-bottom: 30px;
  }

  .ascii-art {
    color: #00ffaa;
    font-size: 9px;
    margin-bottom: 24px;
    text-shadow: 
      0 0 10px rgba(0, 255, 170, 0.6),
      0 0 20px rgba(0, 255, 170, 0.3),
      0 0 30px rgba(0, 255, 170, 0.1);
    filter: drop-shadow(0 0 8px rgba(0, 255, 170, 0.4));
    font-family: 'Fira Code', 'JetBrains Mono', 'SF Mono', 'Monaco', 'Cascadia Code', 'Roboto Mono', Consolas, monospace;
    line-height: 0.9;
    white-space: pre;
    overflow-x: auto;
    font-weight: 400;
    letter-spacing: -0.3px;
    text-align: center;
    background: rgba(0, 255, 170, 0.02);
    padding: 16px;
    border-radius: 8px;
    border: 1px solid rgba(0, 255, 170, 0.15);
    max-width: 100%;
    box-sizing: border-box;
  }

  .welcome-info {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .welcome-line {
    display: flex;
    align-items: center;
    gap: 12px;
    opacity: 0;
    transform: translateY(10px);
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .welcome-line.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .welcome-line .icon {
    font-size: 18px;
    min-width: 24px;
    text-align: center;
    filter: drop-shadow(0 0 5px currentColor);
  }

  .welcome-line strong {
    color: #00d4aa;
    font-weight: 700;
    text-shadow: 0 0 8px rgba(0, 212, 170, 0.4);
  }

  .welcome-line code {
    background: rgba(121, 192, 255, 0.15);
    color: #79c0ff;
    padding: 4px 8px;
    border-radius: 6px;
    font-family: inherit;
    font-size: 14px;
    border: 1px solid rgba(121, 192, 255, 0.3);
    transition: all 0.2s ease;
  }

  .welcome-line code:hover {
    background: rgba(121, 192, 255, 0.25);
    border-color: rgba(121, 192, 255, 0.5);
    transform: translateY(-1px);
  }

  /* Terminal Input */
  .um-terminal-input-line {
    padding: 20px 24px;
    background: rgba(13, 17, 23, 0.8);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    position: relative;
  }

  .input-container {
    display: flex;
    align-items: center;
    gap: 12px;
    position: relative;
  }

  .um-prompt {
    color: #00d4aa;
    font-weight: 700;
    white-space: nowrap;
    text-shadow: 0 0 12px rgba(0, 212, 170, 0.6);
    font-size: 16px;
  }

  .um-input {
    flex: 1;
    background: transparent;
    border: none;
    color: #e6edf3;
    font-family: inherit;
    font-size: 16px;
    outline: none;
    caret-color: transparent; /* Hide default caret */
    padding: 8px 0;
  }

  .um-input::placeholder {
    color: rgba(110, 118, 129, 0.6);
    font-style: italic;
  }

  /* Custom cursor */
  .cursor-blink {
    width: 2px;
    height: 20px;
    background: #00d4aa;
    margin-left: 2px;
    animation: blink 1s infinite;
    box-shadow: 0 0 8px rgba(0, 212, 170, 0.6);
  }

  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  /* Input Suggestions */
  .input-suggestions {
    position: absolute;
    top: -40px;
    left: 0;
    right: 0;
    background: rgba(33, 38, 45, 0.95);
    border: 1px solid rgba(121, 192, 255, 0.3);
    border-radius: 8px;
    padding: 8px 12px;
    font-size: 14px;
    color: #79c0ff;
    backdrop-filter: blur(10px);
    opacity: 0;
    transform: translateY(10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
  }

  .input-suggestions.visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  /* Loading Overlay */
  .loading-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(13, 17, 23, 0.9);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(5px);
    z-index: 100;
  }

  .loading-overlay.visible {
    opacity: 1;
    visibility: visible;
  }

  .loading-spinner {
    width: 40px;
    height: 40px;
    border: 3px solid rgba(121, 192, 255, 0.2);
    border-top: 3px solid #79c0ff;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 16px;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  .loading-text {
    color: #79c0ff;
    font-size: 16px;
    font-weight: 500;
  }

  /* Output styling */
  .command-output {
    margin: 16px 0;
    white-space: pre-wrap;
    word-wrap: break-word;
    line-height: 1.4;
  }

  .command-input {
    color: #e6edf3;
    margin: 12px 0 8px 0;
    font-weight: 600;
  }

  .command-success {
    color: #7ee787;
    text-shadow: 0 0 8px rgba(126, 231, 135, 0.3);
  }

  .command-error {
    color: #f85149;
    text-shadow: 0 0 8px rgba(248, 81, 73, 0.3);
  }

  .command-info {
    color: #79c0ff;
    text-shadow: 0 0 8px rgba(121, 192, 255, 0.3);
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .um-terminal-enhanced {
      width: 90vw;
      height: 65vh;
    }
  }

  @media (max-width: 768px) {
    .um-terminal-enhanced {
      width: 94vw;
      height: 60vh;
      min-height: 400px;
      border-radius: 12px;
    }

    .um-terminal-header {
      padding: 12px 16px;
    }

    .um-control {
      width: 14px;
      height: 14px;
    }

    .um-terminal-title {
      font-size: 12px;
      margin-left: -60px;
    }

    .um-terminal-body {
      padding: 16px;
      font-size: 14px;
    }

    .welcome-line {
      gap: 8px;
    }

    .welcome-line .icon {
      font-size: 16px;
      min-width: 20px;
    }

    .welcome-line code {
      font-size: 12px;
      padding: 2px 6px;
    }

    .um-terminal-input-line {
      padding: 16px;
    }

    .um-prompt {
      font-size: 14px;
    }

    .um-input {
      font-size: 14px;
    }

    .cursor-blink {
      height: 18px;
    }
    
    .ascii-art {
      font-size: 8px;
      line-height: 0.85;
      letter-spacing: -0.35px;
    }
  }

  @media (max-width: 480px) {
    .um-terminal-enhanced {
      width: 99vw;
      height: 55vh;
      min-height: 350px;
      border-radius: 8px;
    }

    .um-terminal-header {
      padding: 10px 12px;
    }

    .um-terminal-title {
      font-size: 11px;
      margin-left: -50px;
    }

    .um-terminal-body {
      padding: 12px;
      font-size: 13px;
    }

    .ascii-art {
      font-size: 7px;
      line-height: 0.8;
      letter-spacing: -0.4px;
      padding: 12px;
    }

    .welcome-line {
      gap: 6px;
      font-size: 13px;
    }

    .welcome-line .icon {
      font-size: 14px;
      min-width: 18px;
    }

    .um-terminal-input-line {
      padding: 12px;
    }

    .um-prompt {
      font-size: 13px;
    }

    .um-input {
      font-size: 13px;
    }

    .cursor-blink {
      height: 16px;
    }
  }

  /* Landscape mobile optimization */
  @media (max-height: 600px) and (orientation: landscape) {
    .um-terminal-enhanced {
      height: 85vh;
      min-height: 300px;
    }

    .um-terminal-body {
      padding: 12px;
    }

    .welcome-message {
      margin-bottom: 16px;
    }

    .welcome-line {
      gap: 8px;
      margin-bottom: 8px;
    }
  }

  /* High DPI displays */
  @media (min-resolution: 2dppx) {
    .um-terminal-enhanced {
      border-width: 1px;
    }

    .um-terminal-enhanced::before {
      top: -1px;
      left: -1px;
      right: -1px;
      bottom: -1px;
    }
  }

  /* Accessibility improvements */
  @media (prefers-reduced-motion: reduce) {
    .um-terminal-enhanced,
    .um-control,
    .welcome-line,
    .cursor-blink,
    .loading-overlay,
    .input-suggestions {
      animation: none;
      transition: none;
    }

    .status-indicator {
      animation: none;
    }
  }

  /* High contrast mode */
  @media (prefers-contrast: high) {
    .um-terminal-enhanced {
      border: 2px solid #ffffff;
      box-shadow: none;
    }

    .um-terminal-enhanced::before {
      display: none;
    }

    .um-terminal-header {
      border-bottom: 2px solid #ffffff;
    }

    .welcome-line strong {
      color: #ffffff;
      text-shadow: none;
    }
  }

  /* ==============================================
   * UI EFFECTS SYSTEM STYLES
   * Advanced animations, themes, and visual feedback
   * ============================================== */

  /* CSS Variables for UI Effects themes */
  :root {
    --terminal-bg: linear-gradient(145deg, #0a0e1a 0%, #1a1f2e 50%, #0d1117 100%);
    --terminal-primary: #00d4aa;
    --terminal-secondary: #79c0ff;
    --terminal-accent: #ff6b6b;
    --terminal-text: #e6edf3;
    --terminal-border: rgba(0, 212, 170, 0.3);
    --terminal-glow: rgba(0, 212, 170, 0.6);
    --animation-speed: 0.3s;
  }

  /* TYPING CURSOR ANIMATION */
  .typing-cursor::after {
    content: '█';
    color: var(--terminal-primary);
    animation: typing-blink 1s infinite;
    text-shadow: 0 0 8px var(--terminal-glow);
  }

  @keyframes typing-blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  /* SYNTAX HIGHLIGHTING */
  .syntax-command {
    color: var(--terminal-primary);
    font-weight: 600;
    text-shadow: 0 0 4px var(--terminal-glow);
  }

  .syntax-option {
    color: var(--terminal-secondary);
    font-style: italic;
  }

  .syntax-number {
    color: var(--terminal-accent);
    font-weight: 500;
  }

  .syntax-string {
    color: #a5d6ff;
  }

  .syntax-email {
    color: #ffab70;
    text-decoration: underline;
    text-decoration-color: rgba(255, 171, 112, 0.5);
  }

  .syntax-url {
    color: #7dd3fc;
    text-decoration: underline;
    text-decoration-color: rgba(125, 211, 252, 0.5);
  }

  .syntax-path {
    color: #86efac;
  }

  /* COMMAND FEEDBACK SYSTEM */
  .command-feedback {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    margin: 4px 0;
    border-radius: 6px;
    font-size: 0.9em;
    opacity: 0;
    transform: translateX(-20px);
    transition: all var(--animation-speed) ease-out;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(10px);
    border-left: 3px solid;
  }

  .command-feedback.show {
    opacity: 1;
    transform: translateX(0);
  }

  .command-feedback.fade-out {
    opacity: 0;
    transform: translateX(20px);
  }

  .command-feedback-success {
    border-left-color: #10b981;
    background: rgba(16, 185, 129, 0.1);
    color: #10b981;
  }

  .command-feedback-error {
    border-left-color: #ef4444;
    background: rgba(239, 68, 68, 0.1);
    color: #ef4444;
  }

  .command-feedback-warning {
    border-left-color: #f59e0b;
    background: rgba(245, 158, 11, 0.1);
    color: #f59e0b;
  }

  .command-feedback-info {
    border-left-color: var(--terminal-secondary);
    background: rgba(121, 192, 255, 0.1);
    color: var(--terminal-secondary);
  }

  .feedback-icon {
    font-size: 1.1em;
    animation: feedback-pulse 2s infinite;
  }

  .feedback-message {
    font-weight: 500;
  }

  @keyframes feedback-pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  /* LOADING INDICATORS */
  .loading-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    padding: 16px;
    margin: 8px 0;
    background: rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    border: 1px solid var(--terminal-border);
    opacity: 0;
    transform: scale(0.9);
    transition: all var(--animation-speed) ease-out;
  }

  .loading-indicator.show {
    opacity: 1;
    transform: scale(1);
  }

  .loading-indicator.fade-out {
    opacity: 0;
    transform: scale(0.9);
  }

  /* Spinner Loader */
  .spinner {
    width: 20px;
    height: 20px;
    position: relative;
  }

  .spinner-circle {
    width: 100%;
    height: 100%;
    border: 2px solid var(--terminal-border);
    border-top: 2px solid var(--terminal-primary);
    border-radius: 50%;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  /* Dots Loader */
  .dots-loader {
    display: flex;
    gap: 4px;
  }

  .dots-loader span {
    width: 8px;
    height: 8px;
    background: var(--terminal-primary);
    border-radius: 50%;
    animation: dots-bounce 1.4s infinite ease-in-out both;
    box-shadow: 0 0 4px var(--terminal-glow);
  }

  .dots-loader span:nth-child(1) { animation-delay: -0.32s; }
  .dots-loader span:nth-child(2) { animation-delay: -0.16s; }
  .dots-loader span:nth-child(3) { animation-delay: 0s; }

  @keyframes dots-bounce {
    0%, 80%, 100% { transform: scale(0); opacity: 0.5; }
    40% { transform: scale(1); opacity: 1; }
  }

  /* Progress Bar */
  .progress-bar {
    width: 200px;
    height: 4px;
    background: var(--terminal-border);
    border-radius: 2px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--terminal-primary), var(--terminal-secondary));
    border-radius: 2px;
    transition: width 0.3s ease;
    box-shadow: 0 0 8px var(--terminal-glow);
  }

  /* Matrix Loader */
  .matrix-loader {
    font-family: monospace;
    font-size: 1.2em;
    color: var(--terminal-primary);
  }

  .matrix-loader span {
    animation: matrix-spin 1s linear infinite;
    text-shadow: 0 0 8px var(--terminal-glow);
  }

  @keyframes matrix-spin {
    0% { content: '|'; }
    25% { content: '/'; }
    50% { content: '-'; }
    75% { content: '\\'; }
    100% { content: '|'; }
  }

  /* Loading Message */
  .loading-message {
    font-size: 0.9em;
    color: var(--terminal-text);
    text-align: center;
    opacity: 0.8;
  }

  /* ENHANCED SUGGESTIONS */
  #input-suggestions {
    position: absolute;
    top: -10px;
    left: 0;
    right: 0;
    background: rgba(0, 0, 0, 0.95);
    border: 1px solid var(--terminal-border);
    border-radius: 8px;
    backdrop-filter: blur(10px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
    transform: translateY(-100%) scale(0.95);
    opacity: 0;
    transition: all var(--animation-speed) ease-out;
    z-index: 1000;
    max-height: 200px;
    overflow: hidden;
  }

  #input-suggestions.visible {
    transform: translateY(-100%) scale(1);
    opacity: 1;
  }

  .suggestions-header {
    padding: 8px 12px;
    border-bottom: 1px solid var(--terminal-border);
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.8em;
    color: var(--terminal-secondary);
  }

  .suggestions-title {
    display: flex;
    align-items: center;
    gap: 4px;
    font-weight: 600;
  }

  .suggestions-count {
    background: var(--terminal-border);
    color: var(--terminal-text);
    padding: 2px 6px;
    border-radius: 10px;
    font-size: 0.8em;
    min-width: 20px;
    text-align: center;
  }

  .suggestions-list {
    max-height: 150px;
    overflow-y: auto;
  }

  .suggestion-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    cursor: pointer;
    transition: all 0.2s ease;
    border-left: 3px solid transparent;
  }

  .suggestion-item:hover,
  .suggestion-item.active {
    background: rgba(0, 212, 170, 0.1);
    border-left-color: var(--terminal-primary);
  }

  .suggestion-icon {
    font-size: 0.9em;
    opacity: 0.7;
  }

  .suggestion-text {
    flex: 1;
    font-family: 'Fira Code', monospace;
    font-size: 0.9em;
  }

  .suggestion-text mark {
    background: var(--terminal-primary);
    color: #000;
    border-radius: 2px;
    padding: 0 2px;
  }

  .suggestion-shortcut {
    font-size: 0.7em;
    opacity: 0.6;
    background: var(--terminal-border);
    padding: 2px 4px;
    border-radius: 2px;
  }

  /* THEME SPECIFIC STYLES */
  /* Matrix Theme */
  .theme-matrix {
    --terminal-bg: linear-gradient(145deg, #000000 0%, #001100 50%, #000000 100%);
    --terminal-primary: #00ff41;
    --terminal-secondary: #00aa00;
    --terminal-accent: #ffff00;
    --terminal-text: #00ff41;
    --terminal-border: rgba(0, 255, 65, 0.5);
    --terminal-glow: rgba(0, 255, 65, 0.8);
  }

  .matrix-rain {
    overflow: hidden;
  }

  .matrix-rain::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(ellipse at center, rgba(0, 255, 65, 0.05) 0%, transparent 70%);
    pointer-events: none;
    z-index: 1;
  }

  /* Retro Theme */
  .theme-retro {
    --terminal-bg: linear-gradient(145deg, #2d1b69 0%, #11001c 50%, #0f0f23 100%);
    --terminal-primary: #ff6ec7;
    --terminal-secondary: #00d9ff;
    --terminal-accent: #ffff00;
    --terminal-text: #ffffff;
    --terminal-border: rgba(255, 110, 199, 0.5);
    --terminal-glow: rgba(255, 110, 199, 0.8);
  }

  .retro-scanlines::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(255, 110, 199, 0.03) 2px,
      rgba(255, 110, 199, 0.03) 4px
    );
    pointer-events: none;
    z-index: 2;
  }

  /* Hacker Theme */
  .theme-hacker {
    --terminal-bg: linear-gradient(145deg, #000000 0%, #0f0f0f 50%, #000000 100%);
    --terminal-primary: #ff0040;
    --terminal-secondary: #00ffff;
    --terminal-accent: #ffff00;
    --terminal-text: #ffffff;
    --terminal-border: rgba(255, 0, 64, 0.5);
    --terminal-glow: rgba(255, 0, 64, 0.8);
  }

  .hacker-glitch {
    position: relative;
  }

  .hacker-glitch::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 48%, rgba(255, 0, 64, 0.02) 49%, rgba(255, 0, 64, 0.02) 51%, transparent 52%);
    pointer-events: none;
    z-index: 1;
  }

  /* FULLSCREEN ENHANCEMENTS */
  .fullscreen-enhanced {
    background: var(--terminal-bg);
  }

  #fullscreen-controls {
    position: fixed;
    top: 20px;
    right: 20px;
    display: flex;
    gap: 12px;
    z-index: 10000;
    animation: slideInFromTop 0.3s ease-out;
  }

  .fullscreen-control-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 12px;
    background: rgba(0, 0, 0, 0.8);
    border: 1px solid var(--terminal-border);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
    backdrop-filter: blur(10px);
    min-width: 60px;
  }

  .fullscreen-control-item:hover {
    background: rgba(0, 212, 170, 0.2);
    border-color: var(--terminal-primary);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px var(--terminal-glow);
  }

  .control-icon {
    font-size: 1.2em;
  }

  .control-label {
    font-size: 0.7em;
    color: var(--terminal-text);
    opacity: 0.8;
    text-transform: uppercase;
    font-weight: 600;
    letter-spacing: 0.5px;
  }

  @keyframes slideInFromTop {
    from {
      transform: translateY(-100px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  /* ANIMATION UTILITIES */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeOut {
    from { opacity: 1; transform: translateY(0); }
    to { opacity: 0; transform: translateY(-10px); }
  }

  @keyframes slideInLeft {
    from { transform: translateX(-100%); }
    to { transform: translateX(0); }
  }

  @keyframes slideInRight {
    from { transform: translateX(100%); }
    to { transform: translateX(0); }
  }

  @keyframes scaleIn {
    from { transform: scale(0.8); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
  }

  @keyframes scaleOut {
    from { transform: scale(1); opacity: 1; }
    to { transform: scale(0.8); opacity: 0; }
  }

  @keyframes glow {
    0%, 100% { text-shadow: 0 0 5px var(--terminal-glow); }
    50% { text-shadow: 0 0 15px var(--terminal-glow); }
  }

  /* ACCESSIBILITY IMPROVEMENTS FOR UI EFFECTS */
  @media (prefers-reduced-motion: reduce) {
    .typing-cursor::after,
    .command-feedback,
    .loading-indicator,
    .spinner-circle,
    .dots-loader span,
    .matrix-loader span,
    #input-suggestions,
    #fullscreen-controls {
      animation: none;
      transition: none;
    }
  }

  /* UI EFFECTS RESPONSIVE ENHANCEMENTS */
  @media (max-width: 768px) {
    .loading-indicator {
      padding: 12px;
      gap: 8px;
    }
    
    .command-feedback {
      font-size: 0.8em;
      padding: 6px 10px;
    }
    
    #input-suggestions {
      font-size: 0.9em;
    }
    
    .suggestion-item {
      padding: 6px 10px;
    }
    
    #fullscreen-controls {
      top: 10px;
      right: 10px;
      gap: 8px;
    }
    
    .fullscreen-control-item {
      padding: 8px;
      min-width: 50px;
    }
    
    .control-icon {
      font-size: 1em;
    }
    
    .control-label {
      font-size: 0.6em;
    }
  }
</style>

<script>
  // Enhanced Terminal Initialization Script - Embedded for Reliability
  class UMTerminalEnhancedInit {
    constructor() {
      this.init();
    }

    async init() {
      // Ensure DOM is ready
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', () => this.initializeTerminal());
      } else {
        this.initializeTerminal();
      }
    }

    async initializeTerminal() {
      try {
        // Check if elements exist
        const container = document.getElementById('um-terminal-enhanced');
        const input = document.getElementById('terminal-input');
        const output = document.getElementById('terminal-output');
        const welcomeAscii = document.getElementById('welcome-ascii');
        
        if (!container || !input || !output) {
          console.warn('UM Terminal Enhanced: Required elements not found');
          return;
        }

        console.log('✓ UM Terminal Enhanced: Elements found, initializing...');

        // Try to load and initialize external enhanced terminal
        try {
          if (window.EnhancedTerminal) {
            const terminal = new window.EnhancedTerminal('um-terminal-enhanced');
            window.umTerminal = terminal;
            console.log('✓ UM Terminal Enhanced: External script initialized');
            return;
          }
        } catch (error) {
          console.warn('UM Terminal Enhanced: External script failed, using fallback:', error);
        }

        // Fallback: Basic terminal functionality with UI Effects
        this.initializeFallbackTerminal(container, input, output, welcomeAscii);
        
      } catch (error) {
        console.error('UM Terminal Enhanced: Initialization failed:', error);
      }
    }

    initializeFallbackTerminal(container, input, output, welcomeAscii) {
      console.log('✓ UM Terminal Enhanced: Using fallback initialization');
      
      // Display ASCII art
      if (welcomeAscii) {
        const asciiArt = `
 ██    ██ ██   ████████ ██ ███    ███  █████      ███    ███ ██ ██       ██        █████  
 ██    ██ ██      ██    ██ ████  ████ ██   ██     ████  ████ ██ ██       ██       ██   ██ 
 ██    ██ ██      ██    ██ ██ ████ ██ ███████     ██ ████ ██ ██ ██       ██       ███████ 
 ██    ██ ██      ██    ██ ██  ██  ██ ██   ██     ██  ██  ██ ██ ██       ██       ██   ██ 
  ██████  ███████ ██    ██ ██      ██ ██   ██     ██      ██ ██ ███████ ███████   ██   ██ 

        ════════════════════════════════════════════════════════════════════════════
        ║  🏢 EMPRESA TECNOLÓGICA LÍDER EN MENDOZA DESDE 2003  🚀              ║
        ║  💻 DESARROLLO WEB • APPS • SISTEMAS • INFRAESTRUCTURA • SEGURIDAD   ║
        ║  📊 +469 PROYECTOS EXITOSOS • +150 CLIENTES SATISFECHOS             ║
        ║  🌐 TERMINAL INTERACTIVO - www.ultimamilla.com.ar                   ║
        ════════════════════════════════════════════════════════════════════════════`;
        
        this.typeText(welcomeAscii, asciiArt, 10);
      }
      
      // Animate welcome lines
      this.animateWelcomeLines();
      
      // Initialize window controls
      this.initializeWindowControls();
      
      // Initialize enhanced terminal functionality with UI Effects
      const terminal = new UMTerminalBasic(container, input, output);
      
      // Initialize UI Effects System
      const uiEffects = new UIEffectsSystem(terminal);
      terminal.uiEffects = uiEffects;
      
      // Store references globally
      window.umTerminal = terminal;
      
      console.log('✅ UM Terminal Professional with UI Effects initialized');
    }
    
    // Typewriter effect
    async typeText(element, text, speed = 15) {
      return new Promise(resolve => {
        element.textContent = '';
        let i = 0;
        const interval = setInterval(() => {
          if (i < text.length) {
            element.textContent += text.charAt(i);
            i++;
          } else {
            clearInterval(interval);
            resolve();
          }
        }, speed);
      });
    }
    
    // Animate welcome lines
    animateWelcomeLines() {
      const welcomeLines = document.querySelectorAll('.welcome-line');
      welcomeLines.forEach((line, index) => {
        const delay = parseInt(line.dataset.delay) || 0;
        setTimeout(() => {
          line.classList.add('visible');
        }, delay);
      });
    }
    
    // Initialize window controls functionality
    initializeWindowControls() {
      const minimizeBtn = document.querySelector('[data-action="minimize"]');
      const maximizeBtn = document.querySelector('[data-action="fullscreen"]');
      const closeBtn = document.querySelector('[data-action="close"]');
      const terminal = document.getElementById('um-terminal-enhanced');
      
      if (minimizeBtn) {
        minimizeBtn.addEventListener('click', () => {
          terminal.style.transform = 'scale(0.1)';
          terminal.style.opacity = '0.3';
          setTimeout(() => {
            terminal.style.transform = 'scale(1)';
            terminal.style.opacity = '1';
          }, 1000);
        });
      }
      
      if (maximizeBtn) {
        let isFullscreen = false;
        maximizeBtn.addEventListener('click', () => {
          if (!isFullscreen) {
            terminal.style.position = 'fixed';
            terminal.style.top = '0';
            terminal.style.left = '0';
            terminal.style.width = '100vw';
            terminal.style.height = '100vh';
            terminal.style.zIndex = '9999';
            terminal.style.borderRadius = '0';
            isFullscreen = true;
          } else {
            terminal.style.position = 'relative';
            terminal.style.top = 'auto';
            terminal.style.left = 'auto';
            terminal.style.width = '';
            terminal.style.height = '';
            terminal.style.zIndex = '';
            terminal.style.borderRadius = '16px';
            isFullscreen = false;
          }
        });
      }
      
      if (closeBtn) {
        closeBtn.addEventListener('click', () => {
          terminal.style.animation = 'fadeOut 0.5s ease forwards';
          setTimeout(() => {
            terminal.style.display = 'none';
            setTimeout(() => {
              terminal.style.display = 'flex';
              terminal.style.animation = 'fadeIn 0.5s ease forwards';
            }, 2000);
          }, 500);
        });
      }
    }
  }
    
  // Basic Terminal functionality as fallback
  class UMTerminalBasic {
    constructor(container, input, output) {
      this.input = input;
      this.output = output;
      this.container = container;
      this.history = [];
      this.historyIndex = -1;
      
      this.setupEventListeners();
      this.focusInput();
      console.log('✅ UM Terminal Basic initialized');
    }
    
    setupEventListeners() {
      this.input.addEventListener('keydown', (e) => {
        switch(e.key) {
          case 'Enter':
            e.preventDefault();
            this.executeCommand();
            break;
          case 'ArrowUp':
            e.preventDefault();
            this.navigateHistory(-1);
            break;
          case 'ArrowDown':
            e.preventDefault();
            this.navigateHistory(1);
            break;
          case 'Tab':
            e.preventDefault();
            this.handleAutocomplete();
            break;
        }
      });
      
      // Click to focus
      document.addEventListener('click', (e) => {
        if (e.target.closest('#um-terminal-enhanced')) {
          this.focusInput();
        }
      });
    }
    
    focusInput() {
      this.input.focus();
    }
    
    executeCommand() {
      const command = this.input.value.trim();
      if (!command) return;
      
      this.history.push(command);
      this.historyIndex = -1;
      
      this.addOutput(`visitante@ultimamilla:~$ ${command}`, 'command-input');
      this.input.value = '';
      
      this.processCommand(command);
      this.scrollToBottom();
    }
    
    processCommand(command) {
      const [cmd, ...args] = command.toLowerCase().split(' ');
      
      switch(cmd) {
        case 'help':
        case 'ayuda':
          this.showHelp();
          break;
        case 'clear':
        case 'cls':
          this.clearOutput();
          break;
        case 'sudo':
          if (args[0] === 'ultimamilla.py') {
            this.showDemo();
          } else {
            this.addOutput('sudo: comando no reconocido', 'command-error');
          }
          break;
        case 'ls':
          this.listDirectory();
          break;
        case 'servicios':
          this.showServices();
          break;
        case 'stats':
          this.showStats();
          break;
        case 'contacto':
          this.showContact();
          break;
        case 'whoami':
          this.addOutput('visitante - Explorador del UM CLI', 'command-success');
          break;
        case 'date':
          this.addOutput(new Date().toString(), 'command-success');
          break;
        case 'matrix':
          this.matrixEffect();
          break;
        case 'theme':
          this.handleThemeCommand(args);
          break;
        case 'sound':
        case 'sonido':
          this.handleSoundCommand();
          break;
        case 'loading':
        case 'cargando':
          this.demoLoadingIndicators();
          break;
        case 'feedback':
          this.demoFeedbackSystem();
          break;
        default:
          this.addOutput(`Comando '${cmd}' no reconocido. Escribe 'help' para ver comandos disponibles.`, 'command-error');
          if (this.uiEffects) {
            this.uiEffects.showCommandFeedback('error', `Comando "${cmd}" no reconocido`);
          }
      }
    }
    
    showHelp() {
      const helpText = `
═══════════════════════════════════════════════════════════════════
                    🚀 ULTIMA MILLA CLI v22.0 - COMANDOS
═══════════════════════════════════════════════════════════════════

📁 NAVEGACIÓN Y SISTEMA:
  • help, ayuda           - Muestra esta ayuda
  • clear, cls            - Limpia la pantalla
  • ls                    - Lista contenido del directorio
  • whoami               - Usuario actual
  • date                 - Fecha y hora actual

🏢 INFORMACIÓN EMPRESARIAL:
  • servicios            - Lista todos los servicios IT
  • stats                - Estadísticas de la empresa
  • contacto             - Información de contacto
  • sudo ultimamilla.py  - Demostración completa

🎮 ENTRETENIMIENTO:
  • matrix               - Efecto Matrix

🎨 UI EFFECTS:
  • theme [nombre]       - Cambiar tema visual (professional, matrix, retro, hacker)
  • sound                - Activar/desactivar efectos de sonido  
  • loading              - Demo de indicadores de carga
  • feedback             - Demo de sistema de retroalimentación

💡 TIPS:
  - Use TAB para autocompletar comandos
  - Use ↑/↓ para navegar el historial
  - Todos los comandos están basados en datos reales de 22 años

═══════════════════════════════════════════════════════════════════
      `;
      this.addOutput(helpText, 'command-success');
    }
    
    showDemo() {
      this.addOutput('🚀 Iniciando demostración de ULTIMA MILLA...', 'command-info');
      this.addOutput('📊 Empresa líder en Mendoza desde 2003', 'command-success');
      this.addOutput('🏢 +469 proyectos exitosos realizados', 'command-success');
      this.addOutput('👥 +150 clientes satisfechos', 'command-success');
      this.addOutput('🔌 Especializados en: Desarrollo Web, Apps, Sistemas, Redes, Seguridad', 'command-success');
    }
    
    showServices() {
      const services = `
💼 SERVICIOS DE ULTIMA MILLA:

🔐 1. Seguridad Informática
   - Ciberseguridad empresarial
   - Monitoreo 24/7
   - Análisis de vulnerabilidades

🌐 2. Redes y Comunicaciones
   - Infraestructura de red
   - Cableado estructurado
   - Wi-Fi empresarial

💻 3. Software y Servicios
   - Desarrollo web
   - Aplicaciones móviles
   - Sistemas a medida

📈 Contacto: info@ultimamilla.com.ar
      `;
      this.addOutput(services, 'command-success');
    }
    
    showStats() {
      const stats = `
📊 ESTADÍSTICAS DE ULTIMA MILLA:

🔥 Años de experiencia: 22+ años (2003-2025)
💼 Proyectos completados: +469
👥 Clientes satisfechos: +150
🏢 Ubicación: Mendoza, Argentina
🎆 Especializaciones: 5 áreas principales
⭐ Tasa de éxito: 98.5%
📊 Crecimiento anual: 15%
      `;
      this.addOutput(stats, 'command-success');
    }
    
    showContact() {
      const contact = `
📞 CONTACTO - ULTIMA MILLA

🏢 Dirección: Av. San Martín 1234, Mendoza, Argentina
📞 Teléfono: +54 261 123 4567
📧 Email: contacto@ultimamilla.com.ar
🌐 Web: www.ultimamilla.com.ar
🕰️ Horario: Lun-Vie 8:00-18:00

🚀 ¡Transformamos tu negocio con tecnología!
      `;
      this.addOutput(contact, 'command-success');
    }
    
    listDirectory() {
      const dirs = `
📁 Contenido de /ultimamilla:

servicios/          📁 Servicios IT disponibles
proyectos/          📁 Historial de proyectos
clientes/           📁 Cartera de clientes
equipo/             📁 Nuestro equipo
empresa.info        📄 Información corporativa
contacto.txt        📄 Datos de contacto
README.md           📄 Documentación
      `;
      this.addOutput(dirs, 'command-success');
    }
    
    clearOutput() {
      const welcome = this.output.querySelector('.welcome-message');
      this.output.innerHTML = '';
      if (welcome) {
        this.output.appendChild(welcome);
      }
    }
    
    matrixEffect() {
      this.addOutput('🟢 Iniciando efecto Matrix...', 'command-info');
      const matrixText = '🟢 Wake up, Neo... The Matrix has you... Follow the white rabbit.';
      this.addOutput(matrixText, 'command-success matrix-effect');
    }
    
    navigateHistory(direction) {
      if (this.history.length === 0) return;
      
      if (direction === -1 && this.historyIndex < this.history.length - 1) {
        this.historyIndex++;
      } else if (direction === 1 && this.historyIndex > -1) {
        this.historyIndex--;
      }
      
      if (this.historyIndex === -1) {
        this.input.value = '';
      } else {
        this.input.value = this.history[this.historyIndex];
      }
    }
    
    handleAutocomplete() {
      const commands = ['help', 'clear', 'ls', 'servicios', 'stats', 'contacto', 'sudo', 'whoami', 'date', 'matrix', 'theme', 'sound', 'loading', 'feedback'];
      const value = this.input.value.toLowerCase();
      
      const matches = commands.filter(cmd => cmd.startsWith(value));
      if (matches.length === 1) {
        this.input.value = matches[0];
      } else if (matches.length > 1) {
        this.addOutput(`Autocompletado: ${matches.join(', ')}`, 'command-info');
      }
    }
    
    addOutput(text, className = '') {
      const div = document.createElement('div');
      div.className = `command-output ${className}`;
      div.innerHTML = text;
      
      this.output.appendChild(div);
      this.scrollToBottom();
    }
    
    scrollToBottom() {
      this.output.scrollTop = this.output.scrollHeight;
    }
    
    handleThemeCommand(args) {
      if (!this.uiEffects) {
        this.addOutput('Sistema de temas no disponible', 'command-error');
        return;
      }
      
      if (args.length === 0) {
        const themes = Object.keys(this.uiEffects.themes);
        this.addOutput(`🎨 TEMAS DISPONIBLES:\n\n${themes.map(t => `  • ${t}`).join('\n')}\n\nUso: theme <nombre>\nActual: ${this.uiEffects.currentTheme}`, 'command-success');
        return;
      }
      
      const themeName = args[0];
      const success = this.uiEffects.switchTheme(themeName);
      
      if (success) {
        this.addOutput(`✅ Tema cambiado a: ${themeName}`, 'command-success');
        this.uiEffects.showCommandFeedback('success', `Tema "${themeName}" aplicado`);
      } else {
        this.addOutput(`❌ Error: Tema "${themeName}" no encontrado`, 'command-error');
        this.uiEffects.showCommandFeedback('error', `Tema "${themeName}" no existe`);
      }
    }
    
    handleSoundCommand() {
      if (!this.uiEffects) {
        this.addOutput('Sistema de sonido no disponible', 'command-error');
        return;
      }
      
      this.uiEffects.toggleSound();
      const status = this.uiEffects.enableSoundEffects ? 'activado' : 'desactivado';
      this.addOutput(`🔊 Sonido ${status}`, 'command-success');
    }
    
    demoLoadingIndicators() {
      if (!this.uiEffects) {
        this.addOutput('Sistema de indicadores de carga no disponible', 'command-error');
        return;
      }
      
      this.addOutput('🔄 Demostrando indicadores de carga...', 'command-info');
      
      // Demo diferentes tipos de indicadores
      const types = ['spinner', 'dots', 'progress', 'matrix'];
      const messages = ['Cargando servicios...', 'Conectando...', 'Procesando datos...', 'Iniciando Matrix...'];
      
      types.forEach((type, index) => {
        setTimeout(() => {
          this.uiEffects.createLoadingIndicator({
            type: type,
            message: messages[index],
            duration: 3000
          });
        }, index * 1000);
      });
    }
    
    demoFeedbackSystem() {
      if (!this.uiEffects) {
        this.addOutput('Sistema de retroalimentación no disponible', 'command-error');
        return;
      }
      
      this.addOutput('📢 Demostrando sistema de retroalimentación...', 'command-info');
      
      const feedbacks = [
        { type: 'success', message: 'Operación exitosa' },
        { type: 'error', message: 'Error simulado' },
        { type: 'warning', message: 'Advertencia de prueba' },
        { type: 'info', message: 'Información adicional' }
      ];
      
      feedbacks.forEach((feedback, index) => {
        setTimeout(() => {
          this.uiEffects.showCommandFeedback(feedback.type, feedback.message, 4000);
        }, index * 1500);
      });
    }
  }

  // ==============================================
  // UI EFFECTS SYSTEM - Integrated Version
  // Advanced animations, themes, and visual feedback
  // ==============================================
  
  class UIEffectsSystem {
    constructor(terminal) {
        this.terminal = terminal;
        this.currentTheme = 'professional';
        this.enableSoundEffects = false;
        this.audioContext = null;
        this.sounds = {};
        
        this.themes = {
            professional: {
                background: 'linear-gradient(145deg, #0a0e1a 0%, #1a1f2e 50%, #0d1117 100%)',
                primary: '#00d4aa',
                secondary: '#79c0ff',
                accent: '#ff6b6b',
                text: '#e6edf3',
                border: 'rgba(0, 212, 170, 0.3)',
                glow: 'rgba(0, 212, 170, 0.6)'
            },
            matrix: {
                background: 'linear-gradient(145deg, #000000 0%, #001100 50%, #000000 100%)',
                primary: '#00ff41',
                secondary: '#00aa00',
                accent: '#ffff00',
                text: '#00ff41',
                border: 'rgba(0, 255, 65, 0.5)',
                glow: 'rgba(0, 255, 65, 0.8)'
            },
            retro: {
                background: 'linear-gradient(145deg, #2d1b69 0%, #11001c 50%, #0f0f23 100%)',
                primary: '#ff6ec7',
                secondary: '#00d9ff',
                accent: '#ffff00',
                text: '#ffffff',
                border: 'rgba(255, 110, 199, 0.5)',
                glow: 'rgba(255, 110, 199, 0.8)'
            },
            hacker: {
                background: 'linear-gradient(145deg, #000000 0%, #0f0f0f 50%, #000000 100%)',
                primary: '#ff0040',
                secondary: '#00ffff',
                accent: '#ffff00',
                text: '#ffffff',
                border: 'rgba(255, 0, 64, 0.5)',
                glow: 'rgba(255, 0, 64, 0.8)'
            }
        };
        
        this.init();
    }
    
    async init() {
        // Load saved theme
        const savedTheme = localStorage.getItem('um-terminal-theme');
        if (savedTheme && this.themes[savedTheme]) {
            this.currentTheme = savedTheme;
        }
        
        // Initialize sound effects
        await this.loadSoundEffects();
        
        // Apply current theme
        this.switchTheme(this.currentTheme);
        
        console.log('✅ UI Effects System initialized');
    }
    
    // THEME SYSTEM
    switchTheme(themeName) {
        if (!this.themes[themeName]) {
            console.warn(`Theme ${themeName} not found`);
            return false;
        }

        this.currentTheme = themeName;
        const theme = this.themes[themeName];
        
        // Apply theme to terminal
        const terminal = document.getElementById('um-terminal-enhanced');
        if (terminal) {
            // Update CSS variables
            const root = document.documentElement;
            root.style.setProperty('--terminal-bg', theme.background);
            root.style.setProperty('--terminal-primary', theme.primary);
            root.style.setProperty('--terminal-secondary', theme.secondary);
            root.style.setProperty('--terminal-accent', theme.accent);
            root.style.setProperty('--terminal-text', theme.text);
            root.style.setProperty('--terminal-border', theme.border);
            root.style.setProperty('--terminal-glow', theme.glow);
            
            // Add theme class
            terminal.className = terminal.className.replace(/theme-\w+/g, '');
            terminal.classList.add(`theme-${themeName}`);
            
            // Special effects for certain themes
            this.applyThemeEffects(themeName);
        }

        // Save preference
        localStorage.setItem('um-terminal-theme', themeName);
        
        return true;
    }

    applyThemeEffects(themeName) {
        const terminal = document.getElementById('um-terminal-enhanced');
        if (!terminal) return;

        // Remove existing effect classes
        terminal.classList.remove('matrix-rain', 'retro-scanlines', 'hacker-glitch');
        
        // Clear existing intervals
        if (terminal.dataset.matrixInterval) {
            clearInterval(terminal.dataset.matrixInterval);
            delete terminal.dataset.matrixInterval;
        }
        if (terminal.dataset.glitchInterval) {
            clearInterval(terminal.dataset.glitchInterval);
            delete terminal.dataset.glitchInterval;
        }
        
        // Remove existing canvas
        const existingCanvas = document.getElementById('matrix-rain-canvas');
        if (existingCanvas) {
            existingCanvas.remove();
        }

        switch (themeName) {
            case 'matrix':
                terminal.classList.add('matrix-rain');
                this.startMatrixRain();
                break;
                
            case 'retro':
                terminal.classList.add('retro-scanlines');
                break;
                
            case 'hacker':
                terminal.classList.add('hacker-glitch');
                this.startHackerGlitch();
                break;
        }
    }

    // SPECIAL THEME EFFECTS
    startMatrixRain() {
        const canvas = document.createElement('canvas');
        canvas.id = 'matrix-rain-canvas';
        canvas.style.position = 'absolute';
        canvas.style.top = '0';
        canvas.style.left = '0';
        canvas.style.width = '100%';
        canvas.style.height = '100%';
        canvas.style.pointerEvents = 'none';
        canvas.style.zIndex = '1';
        canvas.style.opacity = '0.1';

        const terminal = document.getElementById('um-terminal-enhanced');
        if (terminal) {
            terminal.style.position = 'relative';
            terminal.appendChild(canvas);

            const ctx = canvas.getContext('2d');
            canvas.width = terminal.offsetWidth;
            canvas.height = terminal.offsetHeight;

            const matrix = '01ABCDEFGHIJKLMNOPQRSTUVWXYZ';
            const drops = [];
            const columns = canvas.width / 15;

            for (let i = 0; i < columns; i++) {
                drops[i] = 1;
            }

            const draw = () => {
                ctx.fillStyle = 'rgba(0, 0, 0, 0.04)';
                ctx.fillRect(0, 0, canvas.width, canvas.height);

                ctx.fillStyle = '#00ff41';
                ctx.font = '15px monospace';

                for (let i = 0; i < drops.length; i++) {
                    const text = matrix[Math.floor(Math.random() * matrix.length)];
                    ctx.fillText(text, i * 15, drops[i] * 15);

                    if (drops[i] * 15 > canvas.height && Math.random() > 0.975) {
                        drops[i] = 0;
                    }
                    drops[i]++;
                }
            };

            const matrixInterval = setInterval(draw, 35);
            
            // Store interval for cleanup
            terminal.dataset.matrixInterval = matrixInterval;
        }
    }

    startHackerGlitch() {
        const terminal = document.getElementById('um-terminal-enhanced');
        if (!terminal) return;

        const glitch = () => {
            if (Math.random() < 0.1) {
                terminal.style.filter = 'hue-rotate(' + Math.random() * 360 + 'deg) saturate(2)';
                terminal.style.transform = 'translate(' + (Math.random() * 4 - 2) + 'px, ' + (Math.random() * 4 - 2) + 'px)';
                
                setTimeout(() => {
                    terminal.style.filter = '';
                    terminal.style.transform = '';
                }, 100);
            }
        };

        const glitchInterval = setInterval(glitch, 500);
        terminal.dataset.glitchInterval = glitchInterval;
    }

    // SOUND EFFECTS SYSTEM
    async loadSoundEffects() {
        if (!this.enableSoundEffects) return;

        try {
            // Create simple beep sounds using Web Audio API
            this.audioContext = new (window.AudioContext || window.webkitAudioContext)();
            
            this.sounds = {
                keypress: () => this.createBeep(800, 50),
                success: () => this.createBeep(1000, 200),
                error: () => this.createBeep(400, 300),
                notification: () => this.createBeep(600, 150)
            };
            
        } catch (error) {
            console.warn('Sound effects not available:', error);
            this.enableSoundEffects = false;
        }
    }

    createBeep(frequency, duration) {
        if (!this.audioContext) return;
        
        const oscillator = this.audioContext.createOscillator();
        const gainNode = this.audioContext.createGain();
        
        oscillator.connect(gainNode);
        gainNode.connect(this.audioContext.destination);
        
        oscillator.frequency.value = frequency;
        oscillator.type = 'square';
        
        gainNode.gain.setValueAtTime(0, this.audioContext.currentTime);
        gainNode.gain.linearRampToValueAtTime(0.1, this.audioContext.currentTime + 0.01);
        gainNode.gain.exponentialRampToValueAtTime(0.001, this.audioContext.currentTime + duration / 1000);
        
        oscillator.start(this.audioContext.currentTime);
        oscillator.stop(this.audioContext.currentTime + duration / 1000);
    }

    playSound(type) {
        if (!this.enableSoundEffects || !this.sounds[type]) return;
        
        try {
            if (typeof this.sounds[type] === 'function') {
                this.sounds[type]();
            }
        } catch (error) {
            console.warn('Failed to play sound:', error);
        }
    }

    // COMMAND FEEDBACK SYSTEM
    showCommandFeedback(type, message, duration = 3000) {
        const output = document.getElementById('terminal-output');
        if (!output) return;
        
        const feedback = document.createElement('div');
        feedback.className = `command-feedback command-feedback-${type}`;
        
        const icons = {
            success: '✅',
            error: '❌',
            warning: '⚠️',
            info: 'ℹ️'
        };
        
        feedback.innerHTML = `
            <span class="feedback-icon">${icons[type] || 'ℹ️'}</span>
            <span class="feedback-message">${message}</span>
        `;
        
        output.appendChild(feedback);
        
        // Show animation
        setTimeout(() => feedback.classList.add('show'), 10);
        
        // Auto-remove
        setTimeout(() => {
            feedback.classList.add('fade-out');
            setTimeout(() => feedback.remove(), 300);
        }, duration);
        
        // Play sound
        this.playSound(type === 'success' ? 'success' : type === 'error' ? 'error' : 'notification');
    }

    // LOADING INDICATORS
    createLoadingIndicator(options = {}) {
        const {
            type = 'spinner',
            message = 'Cargando...',
            duration = 0
        } = options;
        
        const output = document.getElementById('terminal-output');
        if (!output) return null;
        
        const indicator = document.createElement('div');
        indicator.className = 'loading-indicator';
        
        let loaderHTML = '';
        
        switch (type) {
            case 'spinner':
                loaderHTML = `
                    <div class="spinner">
                        <div class="spinner-circle"></div>
                    </div>
                `;
                break;
                
            case 'dots':
                loaderHTML = `
                    <div class="dots-loader">
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                `;
                break;
                
            case 'progress':
                loaderHTML = `
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 0%"></div>
                    </div>
                `;
                break;
                
            case 'matrix':
                loaderHTML = `
                    <div class="matrix-loader">
                        <span>█</span>
                    </div>
                `;
                break;
        }
        
        indicator.innerHTML = `
            ${loaderHTML}
            <div class="loading-message">${message}</div>
        `;
        
        output.appendChild(indicator);
        
        // Show animation
        setTimeout(() => indicator.classList.add('show'), 10);
        
        // Auto-remove if duration is set
        if (duration > 0) {
            setTimeout(() => {
                indicator.classList.add('fade-out');
                setTimeout(() => indicator.remove(), 300);
            }, duration);
        }
        
        return indicator;
    }

    // COMMAND SUGGESTIONS ENHANCEMENT
    enhanceAutocomplete(input, suggestions = []) {
        const suggestionsContainer = document.getElementById('input-suggestions');
        if (!suggestionsContainer) return;

        if (suggestions.length === 0) {
            suggestionsContainer.classList.remove('visible');
            return;
        }

        // Create enhanced suggestions HTML
        const suggestionsHTML = suggestions.slice(0, 5).map((suggestion, index) => {
            const isActive = index === 0;
            return `
                <div class="suggestion-item ${isActive ? 'active' : ''}" data-suggestion="${suggestion}">
                    <span class="suggestion-icon">⚡</span>
                    <span class="suggestion-text">${this.highlightMatch(suggestion, input)}</span>
                    <span class="suggestion-shortcut">Tab</span>
                </div>
            `;
        }).join('');

        suggestionsContainer.innerHTML = `
            <div class="suggestions-header">
                <span class="suggestions-title">💡 Sugerencias</span>
                <span class="suggestions-count">${suggestions.length}</span>
            </div>
            <div class="suggestions-list">
                ${suggestionsHTML}
            </div>
        `;

        suggestionsContainer.classList.add('visible');

        // Add click handlers
        suggestionsContainer.querySelectorAll('.suggestion-item').forEach(item => {
            item.addEventListener('click', () => {
                const suggestion = item.dataset.suggestion;
                if (this.terminal && this.terminal.input) {
                    this.terminal.input.value = suggestion;
                    this.terminal.focusInput();
                    suggestionsContainer.classList.remove('visible');
                }
            });
        });
    }

    highlightMatch(text, query) {
        if (!query) return text;
        const regex = new RegExp(`(${query})`, 'gi');
        return text.replace(regex, '<mark>$1</mark>');
    }

    // UTILITY METHODS
    cycleTheme() {
        const themes = Object.keys(this.themes);
        const currentIndex = themes.indexOf(this.currentTheme);
        const nextIndex = (currentIndex + 1) % themes.length;
        const nextTheme = themes[nextIndex];
        
        this.switchTheme(nextTheme);
        this.showCommandFeedback('info', `Tema cambiado: ${nextTheme}`, 2000);
    }
    
    toggleSound() {
        this.enableSoundEffects = !this.enableSoundEffects;
        localStorage.setItem('um-terminal-sound', this.enableSoundEffects);
        this.showCommandFeedback('info', `Sonido: ${this.enableSoundEffects ? 'activado' : 'desactivado'}`, 2000);
    }
  }
  
  // Initialize the enhanced terminal system
  new UMTerminalEnhancedInit();
  
  // Add some fade effects for animations
  const style = document.createElement('style');
  style.textContent = `
    @keyframes fadeOut {
      from { opacity: 1; transform: scale(1); }
      to { opacity: 0; transform: scale(0.8); }
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.8); }
      to { opacity: 1; transform: scale(1); }
    }
  `;
  document.head.appendChild(style);
</script>
