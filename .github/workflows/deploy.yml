name: Legacy Docker Image Deploy (disabled)

on:
  workflow_dispatch:

# Superseded by .github/workflows/production-deploy.yml on master.

jobs:
  disabled:
    name: Disabled legacy workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy DockerHub deploy workflow is disabled."
          echo "Production deploys now run from production-deploy.yml on master."
