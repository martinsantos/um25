name: Legacy Docker CI/CD (disabled)

on:
  workflow_dispatch:

# Superseded by the active Git Flow workflows:
# - PR checks: .github/workflows/pr-checks.yml (develop/master)
# - Production deploy: .github/workflows/production-deploy.yml (master)

jobs:
  disabled:
    name: Disabled legacy workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy main-branch Docker CI/CD workflow is disabled."
          echo "Use pr-checks.yml for develop/master PRs and production-deploy.yml for master deploys."
