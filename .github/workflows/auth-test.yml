name: Legacy Authentication Test (disabled)

on:
  workflow_dispatch:

# Superseded by the active Git Flow checks in .github/workflows/pr-checks.yml.

jobs:
  disabled:
    name: Disabled legacy workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy authentication workflow is disabled."
          echo "Use the active PR checks for develop/master validation."
