// @ts-check

/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  // Configuración básica
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'jsdom',
  testMatch: [
    '**/__tests__/**/*.[jt]s?(x)',
    '**/?(*.)+(spec|test).[jt]s?(x)'
  ],
  testPathIgnorePatterns: [
    '/node_modules/',
    '/.astro/',
    '/.netlify/',
    '/dist/'
  ],
  
  // Configuración de módulos
  moduleFileExtensions: ['js', 'jsx', 'ts', 'tsx', 'json', 'node'],
  moduleNameMapper: {
    // Alias de rutas
    '^@/(.*)$': '<rootDir>/src/$1',
    '^@components/(.*)$': '<rootDir>/src/components/$1',
    '^@pages/(.*)$': '<rootDir>/src/pages/$1',
    '^@utils/(.*)$': '<rootDir>/src/utils/$1',
    '^@tests/(.*)$': '<rootDir>/tests/$1',
    
    // Mocks para archivos estáticos
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
    '\\.(jpg|jpeg|png|gif|eot|otf|webp|svg|ttf|woff|woff2|mp4|webm|wav|mp3|m4a|aac|oga)$':
      '<rootDir>/__mocks__/fileMock.js',
    '^astro$': '<rootDir>/__mocks__/astro.js'
  },
  
  // Configuración de transformación
  transform: {
    '^.+\\.(js|jsx|ts|tsx)$': ['babel-jest', { rootMode: 'upward' }],
    '^.+\\.astro$': './jest-astro-transformer.cjs',
    '^.+\\.(css|styl|less|sass|scss|png|jpg|ttf|woff|woff2)$': 'jest-transform-stub'
  },
  transformIgnorePatterns: [
    '/node_modules/(?!(astro|@astrojs|@testing-library|@babel/runtime|@emotion|uuid|@?react)/)',
    '^.+\\.module\\.(css|sass|scss)$'
  ],
  
  // Configuración para setup
  setupFiles: [
    '<rootDir>/jest.setup.js' // Configuración inicial
  ],
  setupFilesAfterEnv: [
    '@testing-library/jest-dom', // Extiende expect con matchers personalizados
    'jest-expect-message' // Mejores mensajes de error
  ],
  
  // Otras configuraciones
  verbose: true,
  bail: false, // Mostrar todos los fallos
  testEnvironmentOptions: {
    url: 'http://localhost',
    customExportConditions: ['node', 'node-addons'],
    userAgent: 'Node.js',
    pretendToBeVisual: true
  },
  
  // Configuración de TypeScript
  globals: {
    'ts-jest': {
      tsconfig: 'tsconfig.jest.json',
      isolatedModules: true,
      useESM: true,
      babelConfig: true
    }
  },
  
  // Configuración de cobertura
  collectCoverage: true,
  collectCoverageFrom: [
    'src/**/*.{js,jsx,ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.stories.{js,jsx,ts,tsx}',
    '!src/main.jsx',
    '!src/main.tsx',
    '!src/pages/**/*',
    '!src/**/index.{js,ts,tsx}',
    '!src/**/*.stories.{js,ts,tsx}',
    '!src/**/*.test.{js,ts,tsx}',
    '!src/**/*.spec.{js,ts,tsx}'
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov', 'clover', 'json', 'html'],
  coveragePathIgnorePatterns: [
    '/node_modules/'
  ],
  coverageThreshold: {
    global: {
      branches: 70,
      functions: 70,
      lines: 70,
      statements: 70
    }
  },
  
  // Configuración para watch mode
  watchPlugins: [
    'jest-watch-typeahead/filename',
    'jest-watch-typeahead/testname',
    'jest-watch-select-projects',
    'jest-watch-suspend'
  ],
  
  // Configuración para snapshots
  snapshotSerializers: [
    '@emotion/jest/serializer'
  ],
  
  // Configuración de módulos ES
  extensionsToTreatAsEsm: ['.ts', '.tsx', '.mjs'],
  moduleDirectories: ['node_modules', 'src'],
  
  // Configuración para manejar timers
  timers: 'modern',
  
  // Configuración de resolución de módulos
  resolver: 'jest-ts-webcompat-resolver',
  
  // Configuración de reportes
  reporters: [
    'default',
    ['jest-junit', {
      outputDirectory: 'test-results',
      outputName: 'junit.xml',
      ancestorSeparator: ' > ',
      uniqueOutputName: 'false',
      suiteNameTemplate: '{filepath}'
    }]
  ]
};

// Exportar configuración
module.exports = config;
