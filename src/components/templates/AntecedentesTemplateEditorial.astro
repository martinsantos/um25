---
import EvidenceCaseRow from '../um/EvidenceCaseRow.astro';
import SectionHeader from '../um/SectionHeader.astro';
import {
  applyDisplayNameReplacements,
  formatDisplayName,
  truncateEditorialText,
} from '../../utils/editorialContent';
import { resolveReplicaH1 } from '../../utils/replicaProdCopy';
import { extractCaseMetricsFromAntecedente } from '../../utils/caseMetrics';

const antecedentesPath = `${Astro.url.pathname}${Astro.url.search}`;
const antecedentesIndexH1 = resolveReplicaH1(antecedentesPath, 'Evidencia operativa real.');

interface Props {
  pageData: any;
  featuredAntecedente?: any;
  filterChips: any[];
  sectorFilter?: string;
  searchQuery?: string;
  areaFilter?: string;
  unidadFilter?: string;
  pageSize: number;
}

const {
  pageData,
  featuredAntecedente = null,
  filterChips,
  sectorFilter = '',
  searchQuery = '',
  areaFilter = '',
  unidadFilter = '',
  pageSize
} = Astro.props;

const currentUrl = new URL(Astro.request.url);
const spotlight = pageData.spotlightAntecedentes || [];
const archive = pageData.antecedentes || [];
const totalItems = pageData.totalItems || 0;
const archiveTotalItems = pageData.archiveTotalItems ?? Math.max(0, totalItems - spotlight.length);
const institutionalProofTotal = Number(pageData.catalogTotalItems || totalItems || 0);
const institutionalProofShort = institutionalProofTotal >= 500
  ? `${Math.floor(institutionalProofTotal / 100) * 100}+`
  : String(institutionalProofTotal);
const totalPages = Math.max(1, pageData.totalPages || 1);
const currentPage = pageData.currentPage || 1;
const archiveView = ['list', 'grid'].includes(pageData.archiveView) ? pageData.archiveView : 'list';
const archiveSort = ['newest', 'oldest', 'client'].includes(pageData.archiveSort) ? pageData.archiveSort : 'newest';
const feature = featuredAntecedente || spotlight[0];
const secondary = spotlight.slice(1, 3);
const hasResults = totalItems > 0;
const showingStart = archiveTotalItems === 0 ? 0 : ((currentPage - 1) * pageSize) + 1;
const showingEnd = Math.min(currentPage * pageSize, archiveTotalItems);
const hasActiveFilters = Boolean(sectorFilter || searchQuery || areaFilter || unidadFilter);
const isLandingPage = Boolean(pageData.isLandingPage ?? (currentPage === 1 && !hasActiveFilters && spotlight.length > 0));
const preservedSearchParams = Array.from(currentUrl.searchParams.entries()).filter(
  ([key]) => !['q', 'page', 'reset'].includes(key)
);

const buildSectorUrl = (sector: string) => {
  const url = new URL(currentUrl);
  if (sector) url.searchParams.set('sector', sector);
  else url.searchParams.delete('sector');
  url.searchParams.delete('page');
  url.searchParams.delete('reset');
  return `${url.pathname}${url.search}`;
};

const buildPageUrl = (page: number) => {
  const url = new URL(currentUrl);
  url.searchParams.set('page', String(page));
  url.searchParams.delete('reset');
  return `${url.pathname}${url.search}`;
};

const buildArchiveControlUrl = (key: 'view' | 'sort', value: string) => {
  const url = new URL(currentUrl);
  url.searchParams.set(key, value);
  url.searchParams.delete('page');
  url.searchParams.delete('reset');
  return `${url.pathname}${url.search}`;
};

const buildClearUrl = () => {
  const url = new URL(currentUrl);
  ['sector', 'area', 'cliente', 'unidad_negocio', 'q', 'page', 'reset'].forEach((key) => url.searchParams.delete(key));
  return `${url.pathname}${url.search}`;
};

const computePaginationPages = () => {
  if (totalPages <= 5) return Array.from({ length: totalPages }, (_, index) => index + 1);
  if (currentPage <= 3) return [1, 2, 3, 4, 5];
  if (currentPage >= totalPages - 2) return [totalPages - 4, totalPages - 3, totalPages - 2, totalPages - 1, totalPages];
  return [currentPage - 2, currentPage - 1, currentPage, currentPage + 1, currentPage + 2];
};

const paginationPages = computePaginationPages();

const truncate = (value: unknown, length = 170) => truncateEditorialText(value, length);
const projectHref = (item: any) => `/antecedentes/${item.slug}`;
const projectTitle = (item: any) =>
  applyDisplayNameReplacements(item?.displayTitle || item?.Titulo || item?.Cliente || 'Antecedente UMSA', [item?.Cliente]);
const projectDescription = (item: any) =>
  applyDisplayNameReplacements(item?.displayDescription || item?.Descripcion || item?.Titulo || '', [item?.Cliente]);
const projectScope = (item: any) => item?.Area || item?.Unidad_de_negocio || 'Servicios IT';
const projectClient = (item: any) => formatDisplayName(item?.Cliente) || 'Proyecto UMSA';
const publicRecordLabel = (value: unknown) => {
  const label = String(value || '')
    .replace(/\bRegistro\s+[\p{L}]+\b/giu, 'Proyecto técnico')
    .replace(/\bRegistro\b/gi, 'Proyecto')
    .replace(/\bDoc[\p{L}]*\b/giu, 'Técnico')
    .trim();
  return label || 'Proyecto técnico';
};
const projectRecordLabel = (item: any) => publicRecordLabel(item?.curation?.recordLabel);
const projectRecordTone = (item: any) => item?.curation?.recordTone || 'technical-record';
const projectYear = (item: any) => {
  if (item?.displayYear && item.displayYear !== 'Invalid Date') return item.displayYear;
  const raw = item?.Fecha || item?.fecha || item?.anio || item?.year;
  if (!raw) return 'sin fecha';
  if (/invalid date/i.test(String(raw))) return 'sin fecha';
  const date = new Date(raw);
  if (!Number.isNaN(date.getTime())) return String(date.getFullYear());
  const year = String(raw).match(/\b(19\d{2}|20\d{2})\b/)?.[1];
  return year || 'sin fecha';
};
const heroImages = [
  feature,
  ...secondary,
  ...archive,
]
  .filter((item: any) => item?.imageUrl)
  .slice(0, 5);
const archiveTitle = isLandingPage
  ? 'Centro de evidencia operativa'
  : currentPage > 1
    ? `Proyectos técnicos - página ${currentPage}`
    : 'Proyectos técnicos filtrados';
const archiveText = isLandingPage
  ? 'Lectura ejecutiva por cliente, alcance, sector y año para abrir cada dossier con contexto comercial claro.'
  : 'Proyectos reales de ULTIMA MILLA, ordenados por cliente, sector, año e imagen asociada.';
const viewOptions = [
  { id: 'list', label: 'Lista' },
  { id: 'grid', label: 'Miniaturas' },
];
const sortOptions = [
  { id: 'newest', label: 'Más recientes' },
  { id: 'oldest', label: 'Más antiguos' },
  { id: 'client', label: 'Cliente A-Z' },
];
---

<section class="ante-dossier">
  {isLandingPage ? (
    <header class="ante-dossier__hero">
      <div class="um-container ante-dossier__hero-grid">
        <div class="ante-dossier__hero-copy">
          <span class="ante-dossier__mark" aria-hidden="true"></span>
          <h1>{antecedentesIndexH1}</h1>
          <p>
            Un archivo de proyectos reales para evaluar alcance, continuidad, criticidad y experiencia
            en redes, seguridad electrónica, telecomunicaciones, software, soporte y energía para IT.
          </p>
          <div class="ante-dossier__actions">
            <a href="#archivo-antecedentes">Ver proyectos</a>
            <a href="/contacto">Solicitar relevamiento</a>
          </div>
        </div>

        <aside class="ante-dossier__hero-panel" aria-label="Resumen visual de antecedentes">
          {heroImages.length > 0 && (
            <div class="ante-dossier__image-wall" aria-label="Imágenes de antecedentes técnicos">
              {heroImages.map((item: any, index: number) => (
                <figure class={index === 0 ? 'is-main' : ''}>
                  <img
                    src={item.imageUrl}
                    alt={item.Titulo || projectTitle(item)}
                    loading={index === 0 ? 'eager' : 'lazy'}
                    decoding="async"
                    onerror="this.src='/images/default.jpg'"
                  />
                </figure>
              ))}
            </div>
          )}
          <div class="ante-dossier__proof" aria-label="Resumen de antecedentes">
            <div>
              <span>Proyectos</span>
              <strong>{institutionalProofShort}</strong>
            </div>
            <div>
              <span>Trayectoria</span>
              <strong>22+ años</strong>
            </div>
            <div>
              <span>Sectores</span>
              <strong>{Math.max(filterChips.length - 1, 0)} mercados</strong>
            </div>
            <div>
              <span>Soporte</span>
              <strong>24/7</strong>
            </div>
          </div>
        </aside>
      </div>
    </header>
  ) : (
    <header class="ante-dossier__archive-intro">
      <div class="um-container ante-dossier__archive-intro-grid">
        <div>
          <span class="ante-dossier__mark" aria-hidden="true"></span>
          <h1>{currentPage > 1 ? `Antecedentes técnicos. Página ${currentPage}.` : 'Antecedentes técnicos.'}</h1>
          <p>
            Proyectos reales de ULTIMA MILLA, ordenados para consultar cliente, sector,
            año e imagen asociada sin repetir la portada institucional.
          </p>
        </div>
        <aside aria-label="Estado del archivo">
          <span>{showingStart}-{showingEnd}</span>
          <strong>{hasActiveFilters ? `${totalItems} coincidencias` : `${institutionalProofShort} proyectos`}</strong>
          <em>{currentPage > 1 ? `Página ${currentPage} de ${totalPages}` : 'Vista filtrada'}</em>
        </aside>
      </div>
    </header>
  )}

  <section class="ante-dossier__controls" aria-label="Índice de antecedentes técnicos">
      <div class="um-container ante-dossier__controls-grid">
        <div class="ante-dossier__range">
          <strong>{showingStart}-{showingEnd}</strong>
          <span>
            {hasActiveFilters
              ? `de ${totalItems} coincidencias`
              : `de ${institutionalProofShort} proyectos`}
          </span>
        </div>

      <form method="get" action="/antecedentes" class="ante-dossier__search">
        {preservedSearchParams.map(([key, value]) => <input type="hidden" name={key} value={value} />)}
        <label for="ante-dossier-search">Afinar búsqueda</label>
        <div>
          <input id="ante-dossier-search" type="search" name="q" value={searchQuery} placeholder="Cliente o alcance" />
          <button type="submit">Buscar</button>
        </div>
      </form>
    </div>

  </section>

  <nav class="um-container ante-dossier__sector-rail" aria-label="Filtrar antecedentes por sector">
    <div class="ante-dossier__sector-links">
      {filterChips.map((chip, index) => (
        <a
          href={buildSectorUrl(chip.id)}
          class={sectorFilter === chip.id || (chip.id === '' && !sectorFilter) ? 'is-active' : ''}
          aria-current={sectorFilter === chip.id || (chip.id === '' && !sectorFilter) ? 'page' : undefined}
        >
          <span>{String(index).padStart(2, '0')}</span>
          <em>{chip.name}</em>
        </a>
      ))}
      {hasActiveFilters && <a href={buildClearUrl()} class="ante-dossier__clear">Limpiar</a>}
    </div>
  </nav>

  {isLandingPage && (
    <section class="um-container ante-dossier__evidence" aria-labelledby="evidencia-destacada">
      <SectionHeader
        id="evidencia-destacada"
        class="ante-dossier__section-head"
        title="Evidencia destacada"
        text="Casos con contexto, alcance y metadata para entender la escala real del trabajo."
      />

      {!hasResults ? (
        <div class="ante-dossier__empty">
          <h2>No se encontraron proyectos</h2>
          <p>Probá con otro sector o una búsqueda más amplia dentro del archivo.</p>
          <a href={buildClearUrl()}>Ver todos los proyectos</a>
        </div>
      ) : (
        <>
          {feature && (
            <a href={projectHref(feature)} class="um-click-surface ante-dossier__feature">
              <figure>
                <img src={feature.imageUrl} alt={feature.Titulo || projectTitle(feature)} loading="eager" decoding="async" onerror="this.src='/images/default.jpg'" />
              </figure>
              <div class="ante-dossier__feature-body">
                <span>{projectRecordLabel(feature)}</span>
                <h3>{projectTitle(feature)}</h3>
                <p>{truncate(projectDescription(feature), 210)}</p>
                <dl>
                  <div>
                    <dt>Sector</dt>
                    <dd>{projectScope(feature)}</dd>
                  </div>
                  <div>
                    <dt>Cliente</dt>
                    <dd>{projectClient(feature)}</dd>
                  </div>
                  <div>
                    <dt>Año</dt>
                    <dd>{projectYear(feature)}</dd>
                  </div>
                </dl>
                <strong>Ver proyecto</strong>
              </div>
            </a>
          )}

          {secondary.length > 0 && (
            <div class="ante-dossier__secondary" aria-label="Casos secundarios">
              {secondary.map((item: any) => (
                <a href={projectHref(item)} class="um-click-surface ante-dossier__secondary-item">
                  <figure>
                    <img src={item.imageUrl} alt={item.Titulo || projectTitle(item)} loading="lazy" decoding="async" onerror="this.src='/images/default.jpg'" />
                  </figure>
                  <div>
                    <span>{projectRecordLabel(item)}</span>
                    <h3>{projectTitle(item)}</h3>
                    <p>{truncate(projectDescription(item), 128)}</p>
                    <dl>
                      <div>
                        <dt>Sector</dt>
                        <dd>{projectScope(item)}</dd>
                      </div>
                      <div>
                        <dt>Año</dt>
                        <dd>{projectYear(item)}</dd>
                      </div>
                    </dl>
                  </div>
                </a>
              ))}
            </div>
          )}
        </>
      )}
    </section>
  )}

  {archive.length > 0 && (
    <section class:list={['um-container', 'ante-dossier__archive', 'ante-dossier__archive--compact', `ante-dossier__archive--${archiveView}`]} id="archivo-antecedentes" aria-labelledby="archivo-antecedentes-title">
      <div class="ante-dossier__archive-head">
        <h2 id="archivo-antecedentes-title">{archiveTitle}</h2>
        <p>{archiveText}</p>
      </div>

      <div class="ante-dossier__archive-toolbar" aria-label="Controles del archivo de antecedentes">
        <div class="ante-dossier__view-toggle" role="group" aria-label="Vista del archivo">
          <span>Vista</span>
          {viewOptions.map((option) => (
            <a
              href={buildArchiveControlUrl('view', option.id)}
              class={archiveView === option.id ? 'is-active' : ''}
              aria-current={archiveView === option.id ? 'true' : undefined}
              data-view-control={option.id}
            >
              {option.label}
            </a>
          ))}
        </div>
        <div class="ante-dossier__sort-toggle" role="group" aria-label="Orden del archivo">
          <span>Orden</span>
          {sortOptions.map((option) => (
            <a
              href={buildArchiveControlUrl('sort', option.id)}
              class={archiveSort === option.id ? 'is-active' : ''}
              aria-current={archiveSort === option.id ? 'true' : undefined}
              data-sort-control={option.id}
            >
              {option.label}
            </a>
          ))}
        </div>
      </div>

      <div class="ante-dossier__ledger antecedentes-list" data-view={archiveView} data-sort={archiveSort}>
        <div class="ante-dossier__ledger-head" aria-hidden="true">
          <span>Proyecto</span>
          <span>Sector</span>
          <span>Año</span>
          <span>Acción</span>
        </div>
        {archive.map((item: any, index: number) => (
          <EvidenceCaseRow
            href={projectHref(item)}
            number={String(showingStart + index).padStart(2, '0')}
            title={projectTitle(item)}
            description={truncate(projectDescription(item), 150)}
            image={item.imageUrl}
            imageAlt={item.Titulo || projectTitle(item)}
            sector={projectScope(item)}
            year={projectYear(item)}
            imageLoading={index < 6 ? 'eager' : 'lazy'}
            eyebrow={projectRecordLabel(item)}
            recordTone={projectRecordTone(item)}
            metrics={extractCaseMetricsFromAntecedente(item)}
          />
        ))}
      </div>
    </section>
  )}

  {archive.length === 0 && !isLandingPage && (
    <section class="um-container ante-dossier__archive ante-dossier__archive--empty" id="archivo-antecedentes" aria-labelledby="archivo-antecedentes-title">
      <div class="ante-dossier__empty ante-dossier__empty--archive">
        <span class="ante-dossier__mark" aria-hidden="true"></span>
        <h2 id="archivo-antecedentes-title">Vista temporalmente sin proyectos visibles</h2>
        <p>
          El índice no pudo cargar antecedentes para esta vista. Podés volver al listado completo
          o pedir un relevamiento técnico con contexto de sector y alcance.
        </p>
        <div class="ante-dossier__empty-actions">
          <a href="/antecedentes">Reintentar listado</a>
          <a href="/contacto">Solicitar relevamiento</a>
        </div>
      </div>
    </section>
  )}

  {totalPages > 1 && (
    <nav class="um-container ante-dossier__pagination" aria-label="Paginación de antecedentes">
      <span>Página {currentPage} de {totalPages}</span>
      <div>
        {currentPage > 1 && <a href={buildPageUrl(currentPage - 1)}>Anterior</a>}
        {paginationPages.map((pageNum) => (
          <a href={buildPageUrl(pageNum)} class={currentPage === pageNum ? 'is-active' : ''}>{pageNum}</a>
        ))}
        {currentPage < totalPages && <a href={buildPageUrl(currentPage + 1)}>Siguiente</a>}
      </div>
    </nav>
  )}
</section>

<style>
  .ante-dossier {
    --ante-read: 1.1875rem;
    --ante-read-lg: 1.25rem;
    --ante-page: var(--skin-page, #fff);
    --ante-section: var(--skin-section, #fff);
    --ante-alt: #ffffff;
    --ante-text: var(--skin-text, #0b0b0b);
    --ante-muted: var(--skin-muted, #5f6368);
    --ante-line: var(--skin-line, #d9dadd);
    background: var(--ante-page);
    color: var(--ante-text);
    overflow-x: clip;
  }

  .ante-dossier a {
    text-decoration: none;
  }

  .ante-dossier__hero {
    background: var(--ante-section);
    border-bottom: 0;
    padding: clamp(28px, 3.4vw, 46px) 0 clamp(22px, 2.8vw, 34px);
  }

  .ante-dossier__hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.54fr) minmax(360px, 0.46fr);
    gap: clamp(28px, 5vw, 68px);
    align-items: start;
  }

  .ante-dossier__hero-copy {
    min-width: 0;
  }

  .ante-dossier__mark,
  .ante-dossier__section-title span {
    display: block;
    width: 56px;
    height: 3px;
    background: var(--um-red);
  }

  .ante-dossier__hero h1 {
    max-width: 900px;
    margin-top: clamp(16px, 2vw, 24px);
    color: var(--ante-text);
    font-family: var(--um-font-display);
    font-size: clamp(2.5rem, 2.88vw, 2.9rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.1;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .ante-dossier__hero p {
    max-width: 760px;
    margin-top: 12px;
    color: var(--ante-muted);
    font-size: clamp(1.125rem, 1.16vw, 1.25rem);
    line-height: 1.68;
  }

  .ante-dossier__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: clamp(18px, 2.4vw, 26px);
  }

  .ante-dossier__actions a {
    display: inline-flex;
    min-height: 48px;
    align-items: center;
    justify-content: center;
    border: 0;
    padding: 0 22px;
    color: #0b0b0b;
    font-size: 1rem;
    font-weight: 600;
    line-height: 1.2;
    text-decoration: underline;
    text-decoration-color: transparent;
    text-decoration-thickness: 2px;
    text-underline-offset: 0.34em;
    transition:
      background-color 180ms ease,
      color 180ms ease,
      text-decoration-color 180ms ease;
  }

  .ante-dossier__actions a:first-child {
    background: #0b0b0b;
    color: #fff;
  }

  .ante-dossier__actions a + a {
    background: #eef0f2;
    color: #111;
  }

  .ante-dossier__actions a:first-child:hover,
  .ante-dossier__actions a:first-child:focus-visible {
    background: var(--um-red);
    color: #fff;
    text-decoration-color: currentColor;
  }

  .ante-dossier__actions a + a:hover,
  .ante-dossier__actions a + a:focus-visible {
    background: #111;
    color: #fff;
    text-decoration-color: currentColor;
  }

  .ante-dossier__hero-panel {
    display: grid;
    gap: 12px;
    min-width: 0;
  }

  .ante-dossier__image-wall {
    display: grid;
    grid-template-columns: minmax(160px, 1.25fr) repeat(2, minmax(90px, 0.75fr));
    grid-template-rows: repeat(2, minmax(118px, 1fr));
    gap: 8px;
    height: clamp(240px, 22vw, 310px);
    min-width: 0;
    border-radius: 6px;
    background: #0b0b0b;
    box-shadow: none;
  }

  .ante-dossier__image-wall figure {
    min-width: 0;
    min-height: 0;
    margin: 0;
    overflow: hidden;
    background: #111;
  }

  .ante-dossier__image-wall figure.is-main {
    grid-row: 1 / span 2;
  }

  .ante-dossier__image-wall img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: saturate(0.9) contrast(1.04);
  }

  .ante-dossier__proof {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
    background: transparent;
    color: var(--ante-text);
  }

  .ante-dossier__proof div {
    min-width: 0;
    padding: clamp(13px, 1.6vw, 18px);
    border-left: 3px solid var(--um-red);
    border-radius: 4px;
    background: var(--ante-page);
    box-shadow: none;
  }

  .ante-dossier__proof span {
    display: block;
    color: var(--ante-muted);
    font-size: var(--ante-read);
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.35;
  }

  .ante-dossier__proof strong {
    display: block;
    margin-top: 8px;
    color: var(--ante-text);
    font-family: var(--um-font-display);
    font-size: clamp(1.55rem, 2vw, 2rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.04;
    text-wrap: balance;
  }

  .ante-dossier__archive-intro {
    background: #fff;
    padding: clamp(28px, 4.6vw, 58px) 0 clamp(10px, 2vw, 20px);
  }

  .ante-dossier__archive-intro-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.68fr) minmax(260px, 0.32fr);
    gap: clamp(24px, 5vw, 72px);
    align-items: end;
  }

  .ante-dossier__archive-intro h1 {
    max-width: 760px;
    margin-top: clamp(14px, 2vw, 20px);
    color: var(--ante-text);
    font-family: var(--um-font-display);
    font-size: clamp(2.625rem, 2.72vw, 2.95rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.1;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .ante-dossier__archive-intro p {
    max-width: 780px;
    margin-top: 12px;
    color: var(--ante-muted);
    font-size: clamp(1.0625rem, 1.1vw, 1.1875rem);
    line-height: 1.64;
  }

  .ante-dossier__archive-intro aside {
    display: grid;
    justify-items: start;
    border-left: 3px solid var(--um-red);
    padding-left: clamp(18px, 2.2vw, 28px);
  }

  .ante-dossier__archive-intro aside span {
    color: var(--um-red);
    font-family: var(--um-font-display);
    font-size: clamp(1.55rem, 2vw, 2rem);
    font-weight: 600;
    line-height: 1;
  }

  .ante-dossier__archive-intro aside strong,
  .ante-dossier__archive-intro aside em {
    display: block;
    margin-top: 8px;
    color: #111;
    font-size: 1.0625rem;
    font-style: normal;
    font-weight: 700;
    line-height: 1.35;
  }

  .ante-dossier__archive-intro aside em {
    color: var(--ante-muted);
    font-weight: 600;
  }

  .ante-dossier__controls {
    position: relative;
    z-index: 1;
    margin-top: 0;
    margin-bottom: 0;
    background: var(--ante-page);
    backdrop-filter: none;
    border-bottom: 0;
    box-shadow: none;
  }

  .ante-dossier__controls-grid {
    display: grid;
    grid-template-columns: minmax(180px, 0.28fr) minmax(320px, 0.72fr);
    gap: clamp(18px, 4vw, 56px);
    align-items: center;
    padding-top: clamp(16px, 2vw, 24px);
    padding-bottom: clamp(14px, 1.8vw, 20px);
  }

  .ante-dossier__range strong {
    display: block;
    color: var(--ante-text);
    font-family: var(--um-font-display);
    font-size: clamp(1.45rem, 1.9vw, 1.9rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1;
  }

  .ante-dossier__range span,
  .ante-dossier__search label {
    display: block;
    margin-top: 4px;
    color: var(--ante-muted);
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.4;
  }

  .ante-dossier__search {
    min-width: 0;
    justify-self: end;
    width: min(100%, 720px);
    padding: 0;
    background: transparent;
    box-shadow: none;
  }

  .ante-dossier__search label {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
  }

  .ante-dossier__search div {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    min-width: 0;
  }

  .ante-dossier__search input,
  .ante-dossier__search button {
    min-height: 44px;
    border-radius: 0;
    font-family: var(--um-font-body);
    font-size: 1rem;
  }

  .ante-dossier__search input {
    min-width: 0;
    border: 0;
    background: #fff;
    color: var(--ante-text);
    padding: 0 16px;
    box-shadow: inset 0 -1px 0 rgba(216, 221, 227, 0.9);
    transition:
      background-color 180ms ease,
      box-shadow 180ms ease,
      color 180ms ease;
  }

  .ante-dossier__search input:hover,
  .ante-dossier__search input:focus {
    background: #fff;
    box-shadow: inset 0 -2px 0 var(--um-red);
  }

  .ante-dossier__search input:focus {
    outline: 2px solid rgba(220, 38, 38, 0.24);
    outline-offset: 2px;
  }

  .ante-dossier__search button {
    border: 0;
    background: var(--um-red);
    color: #fff;
    padding: 0 22px;
    font-weight: var(--um-weight-strong, 700);
    cursor: pointer;
    transition:
      background-color 180ms ease,
      color 180ms ease;
  }

  .ante-dossier__search button:hover,
  .ante-dossier__search button:focus-visible {
    background: #111;
    color: #fff;
  }

  .ante-dossier__sector-rail {
    display: block;
    position: sticky;
    top: 72px;
    z-index: 11;
    min-width: 0;
    align-items: center;
    margin-bottom: clamp(20px, 2.6vw, 30px);
    padding: 0 0 10px;
    border-top: 0;
    border-bottom: 0;
    background: #fff;
    overflow: hidden;
    -webkit-mask-image: linear-gradient(90deg, #000 0, #000 calc(100% - 42px), transparent 100%);
    mask-image: linear-gradient(90deg, #000 0, #000 calc(100% - 42px), transparent 100%);
    scrollbar-width: none;
  }

  .ante-dossier__sector-rail::-webkit-scrollbar {
    display: none;
  }

  .ante-dossier__sector-links {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: clamp(12px, 1.4vw, 20px);
    min-width: 0;
    max-width: 100%;
    overflow-x: auto;
    overflow-y: hidden;
    overscroll-behavior-inline: contain;
    padding-right: clamp(32px, 5vw, 72px);
    scroll-padding-inline: 18px clamp(32px, 5vw, 72px);
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }

  .ante-dossier__sector-links::-webkit-scrollbar {
    display: none;
  }

  .ante-dossier__sector-links a {
    position: relative;
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 0;
    min-height: 48px;
    padding: 0;
    border: 0;
    background: transparent;
    color: var(--ante-text);
    font-size: 1rem;
    font-weight: var(--um-weight-title, 600);
    line-height: 1.2;
    white-space: nowrap;
    text-decoration: none;
    transition: color 180ms ease;
  }

  .ante-dossier__sector-links a::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 2px;
    background: currentColor;
    opacity: 0;
    transition: opacity 180ms ease;
  }

  .ante-dossier__sector-links a span {
    display: none;
    color: var(--ante-muted);
    font-family: var(--um-font-display);
    font-size: 1rem;
    font-weight: 500;
  }

  .ante-dossier__sector-links a em {
    overflow: visible;
    color: inherit;
    font-size: 1rem;
    font-style: normal;
    line-height: 1.2;
  }

  .ante-dossier__sector-links a.is-active {
    color: var(--um-red);
  }

  .ante-dossier__sector-links a:hover,
  .ante-dossier__sector-links a:focus-visible {
    color: var(--um-red);
  }

  .ante-dossier__sector-links a.is-active span {
    color: var(--um-red);
  }

  .ante-dossier__sector-links a.is-active::after {
    opacity: 1;
  }

  .ante-dossier__clear {
    color: var(--ante-muted);
    font-size: 1rem;
    font-weight: var(--um-weight-title, 600);
    border-bottom: 2px solid var(--um-red);
    padding-bottom: 2px;
    transition: color 180ms ease, background-color 180ms ease;
  }

  .ante-dossier__clear:hover,
  .ante-dossier__clear:focus-visible {
    color: var(--um-red);
  }

  @media (max-width: 1080px) {
    .ante-dossier__sector-links {
      display: flex;
      gap: 18px;
    }
  }

  .ante-dossier__evidence {
    padding-top: clamp(28px, 3.8vw, 50px);
  }

  .ante-dossier__section-head,
  .ante-dossier__archive-head {
    display: grid;
    grid-template-columns: minmax(0, 0.52fr) minmax(280px, 0.48fr);
    gap: clamp(24px, 5vw, 80px);
    align-items: start;
    margin-bottom: clamp(18px, 2.4vw, 28px);
  }

  .ante-dossier__section-head h2,
  .ante-dossier__archive-head h2,
  .ante-dossier__empty h2 {
    color: var(--ante-text);
    font-family: var(--um-font-display);
    font-size: clamp(1.85rem, 2.55vw, 2.7rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.12;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .ante-dossier__section-title h2 {
    margin-top: 18px;
  }

  .ante-dossier__section-head p,
  .ante-dossier__archive-head p,
  .ante-dossier__empty p {
    margin-top: 8px;
    color: var(--ante-muted);
    font-size: var(--ante-read-lg);
    line-height: 1.65;
  }

  .ante-dossier__feature {
    display: grid;
    grid-template-columns: minmax(0, 0.58fr) minmax(360px, 0.42fr);
    min-height: clamp(420px, 36vw, 520px);
    overflow: hidden;
    border: 1px solid #e3e6ea;
    border-radius: 6px;
    background: #fff;
    color: #111;
    box-shadow: none;
  }

  .ante-dossier__feature figure,
  .ante-dossier__secondary figure {
    min-width: 0;
    overflow: hidden;
    background: #111;
  }

  .ante-dossier__feature figure {
    height: 100%;
  }

  .ante-dossier__feature img,
  .ante-dossier__secondary img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    color: transparent;
    filter: grayscale(0.08) contrast(1.04) saturate(0.94);
    transition: none;
  }

  .ante-dossier__feature:hover img,
  .ante-dossier__secondary-item:hover img {
    filter: grayscale(0.08) contrast(1.04) saturate(0.94);
  }

  .ante-dossier__feature-body {
    display: flex;
    min-width: 0;
    flex-direction: column;
    justify-content: center;
    background: linear-gradient(180deg, rgba(255,255,255,0.98), rgba(247,247,247,0.98));
    padding: clamp(28px, 4vw, 48px);
  }

  .ante-dossier__feature-body > span,
  .ante-dossier__secondary-item span {
    display: inline-flex;
    width: fit-content;
    border-top: 0;
    box-shadow: inset 0 3px 0 var(--um-red);
    padding-top: 12px;
    color: var(--um-red);
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.2;
  }

  .ante-dossier__feature h3 {
    margin-top: 18px;
    color: #111;
    font-family: var(--um-font-display);
    font-size: clamp(1.72rem, 2.34vw, 2.45rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.12;
    text-wrap: balance;
  }

  .ante-dossier__feature p {
    margin-top: 16px;
    color: #555d66;
    font-size: clamp(1rem, 1.12vw, 1.12rem);
    line-height: 1.65;
  }

  .ante-dossier__feature dl {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 14px;
    margin-top: 22px;
    background: transparent;
  }

  .ante-dossier__feature dl div {
    min-width: 0;
    background: transparent;
    padding: 0;
    border-top: 0;
    padding-top: 14px;
  }

  .ante-dossier__feature dt,
  .ante-dossier__secondary dt,
  .ante-dossier__row-meta span {
    color: #6b7280;
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.3;
  }

  .ante-dossier__feature dd,
  .ante-dossier__secondary dd,
  .ante-dossier__row-meta strong {
    margin: 6px 0 0;
    color: #111;
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.35;
  }

  .ante-dossier__feature strong {
    display: inline-flex;
    width: fit-content;
    margin-top: 22px;
    color: #111;
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.2;
  }

  .ante-dossier__feature strong::after,
  .ante-dossier__row em::after,
  .ante-dossier__pagination a:not(.is-active)::after {
    content: "";
    width: 18px;
    height: 1px;
    margin-left: 10px;
    background: var(--um-red);
    align-self: center;
  }

  .ante-dossier__secondary {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(14px, 2vw, 20px);
    margin-top: 18px;
  }

  .ante-dossier__secondary-item {
    display: grid;
    grid-template-columns: minmax(180px, 0.42fr) minmax(0, 0.58fr);
    min-height: 250px;
    border: 1px solid #e3e6ea;
    border-radius: 6px;
    background: #fff;
    color: #111;
    box-shadow: none;
    overflow: hidden;
  }

  .ante-dossier__secondary-item > div {
    min-width: 0;
    padding: clamp(22px, 3vw, 34px);
  }

  .ante-dossier__secondary-item span {
    color: var(--um-red);
  }

  .ante-dossier__secondary h3 {
    margin-top: 18px;
    color: #111;
    font-family: var(--um-font-display);
    font-size: clamp(1.38rem, 1.75vw, 1.75rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.15;
    text-wrap: balance;
  }

  .ante-dossier__secondary p {
    margin-top: 12px;
    color: #555;
    font-size: 1rem;
    line-height: 1.58;
  }

  .ante-dossier__secondary dl {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    margin-top: 18px;
    border-top: 0;
    padding-top: 14px;
  }

  .ante-dossier__secondary dt {
    color: #5f6670;
  }

  .ante-dossier__secondary dd {
    color: #111;
  }

  .ante-dossier__archive {
    padding-top: clamp(34px, 5vw, 62px);
    scroll-margin-top: 136px;
  }

  .ante-dossier__archive--compact,
  .ante-dossier__archive--empty {
    padding-top: clamp(18px, 2.6vw, 30px);
  }

  .ante-dossier__archive-head {
    border-top: 0;
    padding-top: clamp(28px, 4vw, 44px);
  }

  .ante-dossier__archive-toolbar {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 12px 18px;
    margin: -2px 0 clamp(14px, 2vw, 22px);
  }

  .ante-dossier__view-toggle,
  .ante-dossier__sort-toggle {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    min-width: 0;
  }

  .ante-dossier__view-toggle > span,
  .ante-dossier__sort-toggle > span {
    color: var(--ante-muted);
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.2;
  }

  .ante-dossier__view-toggle a,
  .ante-dossier__sort-toggle a {
    display: inline-flex;
    min-height: 40px;
    align-items: center;
    justify-content: center;
    border: 1px solid #d9dadd;
    border-radius: 4px;
    background: #fff;
    color: #111;
    padding: 0 13px;
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.15;
    text-decoration: none;
    transition:
      background-color 160ms ease,
      border-color 160ms ease,
      color 160ms ease;
  }

  .ante-dossier__view-toggle a:hover,
  .ante-dossier__view-toggle a:focus-visible,
  .ante-dossier__sort-toggle a:hover,
  .ante-dossier__sort-toggle a:focus-visible {
    border-color: var(--um-red);
    color: var(--um-red);
    outline: none;
  }

  .ante-dossier__view-toggle a.is-active,
  .ante-dossier__sort-toggle a.is-active {
    border-color: #111;
    background: #111;
    color: #fff;
  }

  .ante-dossier__ledger {
    border-top: 0;
    display: grid;
    gap: 0.85rem;
  }

  .ante-dossier__ledger-head {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 56px minmax(320px, 1fr) minmax(170px, 0.2fr) minmax(120px, 0.14fr) auto;
    gap: clamp(18px, 3.5vw, 46px);
    align-items: center;
    margin-bottom: 0;
    border-top: 0;
    border-bottom: 0;
    padding: 10px 0;
    background: var(--ante-page, #fff);
    box-shadow: none;
  }

  .ante-dossier__ledger-head span {
    color: var(--ante-muted);
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    line-height: 1.3;
  }

  .ante-dossier__ledger-head span:first-child {
    grid-column: 2;
  }

  .ante-dossier__archive--grid .ante-dossier__ledger {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(12px, 1.6vw, 18px);
  }

  .ante-dossier__archive--grid .ante-dossier__ledger-head {
    display: none;
  }

  .ante-dossier__archive--grid :global(.evidence-case-row) {
    grid-template-columns: minmax(0, 1fr);
    align-content: start;
    height: 100%;
    margin: 0;
    padding: clamp(14px, 1.6vw, 18px);
  }

  .ante-dossier__archive--grid :global(.evidence-case-row__number),
  .ante-dossier__archive--grid :global(.evidence-case-row__thumb),
  .ante-dossier__archive--grid :global(.evidence-case-row__copy),
  .ante-dossier__archive--grid :global(.evidence-case-row__meta-group),
  .ante-dossier__archive--grid :global(.evidence-case-row__action) {
    grid-column: 1;
    grid-row: auto;
  }

  .ante-dossier__archive--grid :global(.evidence-case-row__thumb) {
    width: 100%;
    min-width: 0;
    min-height: 150px;
    aspect-ratio: 16 / 10;
  }

  .ante-dossier__archive--grid :global(.evidence-case-row__copy) {
    align-self: start;
  }

  .ante-dossier__archive--grid :global(.evidence-case-row__meta-group) {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .ante-dossier__archive--grid :global(.evidence-case-row__action) {
    justify-self: start;
  }

  .ante-dossier__row {
    display: grid;
    grid-template-columns: 56px minmax(320px, 1fr) minmax(170px, 0.2fr) minmax(120px, 0.14fr) auto;
    gap: clamp(18px, 3.5vw, 46px);
    align-items: center;
    border: 1px solid #e3e6ea;
    border-radius: 6px;
    background: #fff;
    padding: clamp(14px, 1.8vw, 20px);
    color: #111;
    box-shadow: none;
    transition:
      background-color 180ms ease,
      border-color 180ms ease;
  }

  .ante-dossier__row:hover {
    border-color: color-mix(in srgb, var(--um-red) 34%, #e3e6ea);
    background: color-mix(in srgb, #ffffff 94%, var(--um-red) 6%);
  }

  .ante-dossier__row-number {
    color: var(--um-red);
    font-family: var(--um-font-display);
    font-size: 1.1rem;
    font-weight: var(--um-weight-title, 600);
    line-height: 1;
  }

  .ante-dossier__row-main {
    display: grid;
    grid-template-columns: 144px minmax(0, 1fr);
    gap: 20px;
    align-items: center;
    min-width: 0;
  }

  .ante-dossier__row-main figure {
    width: 164px;
    height: 112px;
    margin: 0;
    overflow: hidden;
    background: #111;
    border: 0;
    border-radius: 4px;
  }

  .ante-dossier__row-main img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(0.08) contrast(1.04) saturate(0.94);
  }

  .ante-dossier__row h3 {
    color: #111;
    font-family: var(--um-font-display);
    font-size: clamp(1.18rem, 1.45vw, 1.52rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.18;
    text-wrap: balance;
  }

  .ante-dossier__row p {
    max-width: 680px;
    margin-top: 8px;
    color: #5d6269;
    font-size: var(--ante-read);
    line-height: 1.58;
  }

  .ante-dossier__row-meta span {
    display: block;
    color: #5f6670;
  }

  .ante-dossier__row-meta strong {
    display: block;
    color: #111;
  }

  .ante-dossier__row em {
    display: inline-flex;
    align-items: center;
    justify-self: end;
    color: #111;
    font-size: 1rem;
    font-style: normal;
    font-weight: var(--um-weight-strong, 700);
    white-space: nowrap;
  }

  .ante-dossier__pagination {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 18px;
    align-items: center;
    padding-top: 28px;
    padding-bottom: clamp(72px, 9vw, 118px);
  }

  .ante-dossier__pagination span {
    color: var(--ante-muted);
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
  }

  .ante-dossier__pagination div {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ante-dossier__pagination a {
    display: inline-flex;
    min-width: 44px;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    border: 0;
    background: rgba(17,17,17,0.05);
    padding: 0 14px;
    color: #111;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: underline;
    text-decoration-color: transparent;
    text-decoration-thickness: 2px;
    text-underline-offset: 0.35em;
    transition:
      background-color 180ms ease,
      color 180ms ease,
      text-decoration-color 180ms ease;
  }

  .ante-dossier__pagination a.is-active {
    background: #111;
    color: #fff;
  }

  .ante-dossier__pagination a:not(.is-active):hover,
  .ante-dossier__pagination a:not(.is-active):focus-visible {
    background: rgba(220, 38, 38, 0.08);
    color: var(--um-red);
    text-decoration-color: currentColor;
  }

  .ante-dossier__empty {
    border-top: 0;
    border-bottom: 0;
    background: #f7f7f7;
    padding: clamp(30px, 5vw, 62px);
  }

  .ante-dossier__empty--archive {
    display: grid;
    max-width: 880px;
    margin-inline: auto;
    background: linear-gradient(90deg, #ffffff 0%, #f7f8f9 100%);
    border: 1px solid #e3e6ea;
    border-radius: 6px;
    box-shadow: none;
  }

  .ante-dossier__empty a {
    display: inline-flex;
    margin-top: 20px;
    color: #111;
    font-size: 1rem;
    font-weight: var(--um-weight-strong, 700);
    text-decoration: underline;
    text-decoration-color: transparent;
    text-decoration-thickness: 2px;
    text-underline-offset: 0.36em;
    transition: color 180ms ease, text-decoration-color 180ms ease;
  }

  .ante-dossier__empty a:hover,
  .ante-dossier__empty a:focus-visible {
    color: var(--um-red);
    text-decoration-color: currentColor;
  }

  .ante-dossier__empty-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
  }

  @media (max-width: 1080px) {
    .ante-dossier__hero-grid,
    .ante-dossier__archive-intro-grid,
    .ante-dossier__feature,
    .ante-dossier__secondary-item {
      grid-template-columns: 1fr;
    }

    .ante-dossier__feature figure {
      min-height: 360px;
    }

    .ante-dossier__feature-body {
      border-left: 0;
      border-top: 0;
    }

    .ante-dossier__secondary figure {
      min-height: 220px;
    }

    .ante-dossier__row {
      grid-template-columns: 52px minmax(0, 1fr) minmax(150px, 0.24fr) auto;
    }

    .ante-dossier__ledger-head {
      grid-template-columns: 52px minmax(0, 1fr) minmax(150px, 0.24fr) auto;
    }

    .ante-dossier__ledger-head span:nth-child(3) {
      display: none;
    }

    .ante-dossier__archive--grid .ante-dossier__ledger {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .ante-dossier__row-meta:nth-of-type(2) {
      display: none;
    }

    .ante-dossier__search {
      justify-self: stretch;
      width: 100%;
    }

  }

  @media (max-width: 780px) {
    .ante-dossier__hero {
      padding: 48px 0 34px;
    }

    .ante-dossier__hero-grid,
    .ante-dossier__archive-intro-grid,
    .ante-dossier__controls-grid,
    .ante-dossier__section-head,
    .ante-dossier__archive-head,
    .ante-dossier__secondary {
      grid-template-columns: 1fr;
    }

    .ante-dossier__hero h1 {
      max-width: 18ch;
      font-size: clamp(2rem, 7.2vw, 2.32rem);
      line-height: 1.12;
      text-wrap: pretty;
    }

    .ante-dossier__hero p {
      max-width: 34ch;
      line-height: 1.48;
    }

    .ante-dossier__image-wall {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: auto auto;
      height: auto;
    }

    .ante-dossier__image-wall figure {
      min-height: 132px;
      aspect-ratio: 4 / 3;
    }

    .ante-dossier__image-wall figure.is-main {
      grid-column: 1 / -1;
      grid-row: auto;
      aspect-ratio: 16 / 9;
    }

    .ante-dossier__proof {
      grid-template-columns: 1fr 1fr;
    }

    .ante-dossier__archive-toolbar {
      align-items: stretch;
      flex-direction: column;
    }

    .ante-dossier__view-toggle,
    .ante-dossier__sort-toggle {
      width: 100%;
    }

    .ante-dossier__view-toggle a,
    .ante-dossier__sort-toggle a {
      flex: 1 1 auto;
    }

    .ante-dossier__archive--grid .ante-dossier__ledger {
      grid-template-columns: minmax(0, 1fr);
    }

    .ante-dossier__controls {
      margin-top: 0;
      margin-bottom: 0;
      box-shadow: none;
    }

    .ante-dossier__controls-grid {
      gap: 10px;
      padding-top: 10px;
      padding-bottom: 8px;
    }

    .ante-dossier__range {
      display: none;
    }

    .ante-dossier__range strong {
      font-size: 1.25rem;
    }

    .ante-dossier__range span {
      margin-top: 0;
      font-size: 1rem;
      line-height: 1.25;
    }

    .ante-dossier__search label {
      margin-bottom: 6px;
    }

    .ante-dossier__search input,
    .ante-dossier__search button {
      min-height: 44px;
    }

    .ante-dossier__sector-rail {
      top: 64px;
      max-width: 100%;
      margin-inline: auto;
      margin-bottom: 22px;
      padding-bottom: 0;
      border-bottom: 1px solid #e6e8eb;
      -webkit-mask-image: none;
      mask-image: none;
    }

    .ante-dossier__sector-links {
      display: flex;
      flex-wrap: nowrap;
      max-width: 100%;
      gap: 14px;
      padding-inline: 14px 42px;
      scroll-padding-inline: 14px 42px;
      overflow-x: auto;
      overflow-y: hidden;
      scrollbar-width: none;
    }

    .ante-dossier__sector-links a {
      min-height: 42px;
      font-size: 1rem;
    }

    .ante-dossier__sector-links a em {
      font-size: 1rem;
    }

    .ante-dossier__feature {
      height: auto;
      min-height: 0;
      overflow: visible;
    }

    .ante-dossier__feature figure {
      min-height: 260px;
    }

    .ante-dossier__feature dl {
      grid-template-columns: 1fr;
    }

    .ante-dossier__row {
      grid-template-columns: minmax(0, 1fr);
      gap: 0;
      align-items: start;
      padding: 28px 0;
    }

    .ante-dossier__row-main {
      grid-template-columns: minmax(0, 1fr);
      gap: 16px;
      align-items: start;
    }

    .ante-dossier__row-main figure {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 10;
    }

    .ante-dossier__ledger-head {
      display: none;
    }

    .ante-dossier__row-meta,
    .ante-dossier__row em {
      grid-column: 1;
      justify-self: start;
    }

    .ante-dossier__row-meta {
      display: inline-grid;
      grid-template-columns: auto minmax(0, 1fr);
      column-gap: 12px;
      align-items: baseline;
      width: min(100%, 360px);
      margin-top: 14px;
    }

    .ante-dossier__row em {
      margin-top: 12px;
    }

    .ante-dossier__pagination {
      align-items: flex-start;
      flex-direction: column;
    }
  }

  @media (max-width: 620px) {
    .ante-dossier__row {
      grid-template-columns: minmax(0, 1fr);
      gap: 0;
    }

    .ante-dossier__row-main {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .ante-dossier__row-main figure {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 10;
    }
  }

  @media (max-width: 520px) {
    .ante-dossier__hero {
      padding: 38px 0 24px;
    }

    .ante-dossier__hero-grid {
      gap: 22px;
    }

    .ante-dossier__clear {
      white-space: nowrap;
    }

    .ante-dossier__hero h1 {
      max-width: 16.5ch;
      font-size: clamp(2rem, 6.9vw, 2.18rem);
      line-height: 1.14;
      text-wrap: pretty;
    }

    .ante-dossier__archive-intro h1 {
      max-width: 100%;
      font-size: clamp(2.125rem, 7vw, 2.42rem);
    }

    .ante-dossier__archive-intro aside {
      border-left-width: 2px;
      padding-left: 16px;
    }

    .ante-dossier__row-number {
      display: inline-flex;
      width: fit-content;
      margin-bottom: 12px;
      padding-top: 2px;
    }

    .ante-dossier__row-main {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .ante-dossier__row-main figure {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 10;
    }

    .ante-dossier__hero p {
      max-width: 33ch;
      line-height: 1.55;
    }

    .ante-dossier__actions {
      flex-direction: column;
      gap: 10px;
      margin-top: 16px;
    }

    .ante-dossier__actions a {
      width: 100%;
      min-height: 44px;
      padding: 0 16px;
      font-size: 1rem;
    }

    .ante-dossier__search input,
    .ante-dossier__search button {
      min-height: 42px;
      font-size: 1rem;
    }

    .ante-dossier__search input {
      padding: 0 12px;
    }

    .ante-dossier__search button {
      padding: 0 16px;
    }

    .ante-dossier__proof {
      display: none;
    }

    .ante-dossier__evidence {
      padding-top: 24px;
    }

    .ante-dossier__section-title h2 {
      margin-top: 12px;
    }

    .ante-dossier__section-head p {
      max-width: 34ch;
    }

    .ante-dossier__feature-body {
      padding: 24px 18px;
    }

    .ante-dossier__feature h3 {
      font-size: clamp(1.42rem, 6.2vw, 1.72rem);
      line-height: 1.16;
    }

    .ante-dossier__feature p {
      line-height: 1.58;
    }

    .ante-dossier__secondary dl {
      grid-template-columns: 1fr;
      gap: 12px;
    }
  }
</style>

<script>
  document.querySelectorAll('.ante-dossier__sector-links').forEach((rail) => {
    const active = rail.querySelector('.is-active');
    if (active instanceof HTMLElement) {
      const isCompact = window.matchMedia('(max-width: 720px)').matches;
      const targetLeft = isCompact
        ? active.offsetLeft - 14
        : active.offsetLeft - Math.max(0, (rail.clientWidth - active.clientWidth) / 2);

      rail.scrollLeft = Math.max(0, targetLeft);
    }
  });
</script>
