---
import Layout from '../../layouts/Layout.astro';
import OptimizedImage from '../../components/OptimizedImage.astro';
import BlogSidebar from '../../components/BlogSidebar.astro';
import Comments from '../../components/Comments.astro';
import { getAllPosts } from '../../data/blog';
import { formatDate } from '../../utils/date';

export const getStaticPaths = (() => {
    const posts = getAllPosts();
    return posts.map(post => ({
        params: { slug: post.slug },
        props: { post },
    }));
});

const { post } = Astro.props;

// Botones de compartir
const shareUrl = new URL(Astro.url.pathname, Astro.site).toString();
const shareText = `Lee "${post.title}" en Última Milla`;
const socialLinks = [
    {
        name: 'Twitter',
        url: `https://twitter.com/intent/tweet?text=${encodeURIComponent(shareText)}&url=${encodeURIComponent(shareUrl)}`,
        icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M8.29 20.251c7.547 0 11.675-6.253 11.675-11.675 0-.178 0-.355-.012-.53A8.348 8.348 0 0022 5.92a8.19 8.19 0 01-2.357.646 4.118 4.118 0 001.804-2.27 8.224 8.224 0 01-2.605.996 4.107 4.107 0 00-6.993 3.743 11.65 11.65 0 01-8.457-4.287 4.106 4.106 0 001.27 5.477A4.072 4.072 0 012.8 9.713v.052a4.105 4.105 0 003.292 4.022 4.095 4.095 0 01-1.853.07 4.108 4.108 0 003.834 2.85A8.233 8.233 0 012 18.407a11.616 11.616 0 006.29 1.84"></path>
        </svg>`
    },
    {
        name: 'LinkedIn',
        url: `https://www.linkedin.com/shareArticle?mini=true&url=${encodeURIComponent(shareUrl)}&title=${encodeURIComponent(post.title)}`,
        icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"></path>
        </svg>`
    },
    {
        name: 'Facebook',
        url: `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(shareUrl)}`,
        icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path fill-rule="evenodd" d="M22 12c0-5.523-4.477-10-10-10S2 6.477 2 12c0 4.991 3.657 9.128 8.438 9.878v-6.987h-2.54V12h2.54V9.797c0-2.506 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V12h2.773l-.443 2.89h-2.33v6.988C18.343 21.128 22 16.991 22 12z" clip-rule="evenodd"></path>
        </svg>`
    },
    {
        name: 'WhatsApp',
        url: `https://wa.me/?text=${encodeURIComponent(`${shareText} ${shareUrl}`)}`,
        icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path fill-rule="evenodd" d="M20.11 3.89C17.95 1.72 14.99.5 11.99.5 5.81.5.5 5.81.5 12c0 2.14.59 4.19 1.71 5.99L.5 23.5l5.71-1.72c1.73 1.05 3.68 1.62 5.78 1.62 6.18 0 11.49-5.31 11.49-11.5 0-3-1.22-5.96-3.39-8.13zM12 21.85c-1.89 0-3.74-.48-5.35-1.4l-.39-.23-3.97 1.19 1.19-3.97-.25-.4c-1.02-1.62-1.58-3.49-1.58-5.44 0-5.42 4.41-9.83 9.83-9.83 2.63 0 5.09 1.02 6.95 2.88 1.86 1.86 2.88 4.32 2.88 6.95 0 5.42-4.41 9.83-9.83 9.83zm5.48-7.26c-.3-.15-1.77-.87-2.04-.97-.27-.1-.47-.15-.67.15-.2.3-.77.97-.95 1.17-.17.2-.35.22-.65.07-.3-.15-1.26-.46-2.4-1.48-.89-.79-1.49-1.77-1.66-2.07-.17-.3-.02-.46.13-.61.13-.13.3-.35.45-.52.15-.17.2-.3.3-.5.1-.2.05-.37-.02-.52-.07-.15-.67-1.62-.92-2.22-.24-.58-.48-.5-.67-.51-.17-.01-.37-.01-.57-.01-.2 0-.52.07-.8.37-.27.3-1.04.97-1.04 2.37 0 1.4.97 2.74 1.1 2.94.13.2 1.86 3.02 4.6 4.11.64.28 1.14.44 1.53.57.64.2 1.23.17 1.69.1.52-.07 1.6-.65 1.82-1.27.22-.62.22-1.15.15-1.27-.07-.12-.27-.19-.57-.34z" clip-rule="evenodd"></path>
        </svg>`
    }
];
---

<Layout title={`${post.title} | Blog | Última Milla`}>
    <main class="container mx-auto px-4 py-12">
        <div class="max-w-7xl mx-auto">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
                <!-- Contenido Principal -->
                <article class="lg:col-span-2">
                    <header class="mb-8">
                        <div class="flex flex-wrap gap-2 mb-4">
                            {post.categories?.map(category => (
                                <a
                                    href={`/blog?category=${encodeURIComponent(category)}`}
                                    class="px-3 py-1 bg-primary-100 text-primary-700 rounded-full text-sm hover:bg-primary-200 transition-colors"
                                >
                                    {category}
                                </a>
                            ))}
                        </div>
                        <h1 class="text-4xl font-bold text-gray-900 mb-4">{post.title}</h1>
                        <div class="flex items-center gap-4 text-gray-600">
                            <time datetime={post.date} class="flex items-center gap-1">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd" />
                                </svg>
                                {formatDate(post.date)}
                            </time>
                            <span class="flex items-center gap-1">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
                                </svg>
                                {post.author}
                            </span>
                        </div>
                    </header>

                    <div class="prose prose-lg max-w-none mb-12">
                        <div class="mb-8 rounded-lg overflow-hidden">
                            <OptimizedImage
                                src={post.image}
                                alt={post.title}
                                class="w-full h-auto"
                            />
                        </div>
                        
                        <div set:html={post.content} />
                    </div>

                    <!-- Botones de compartir -->
                    <div class="border-t border-b border-gray-200 py-6 mb-12">
                        <h3 class="text-lg font-semibold mb-4">Comparte este artículo</h3>
                        <div class="flex gap-4">
                            {socialLinks.map(link => (
                                <a
                                    href={link.url}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="p-2 text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-full transition-colors"
                                    aria-label={`Compartir en ${link.name}`}
                                >
                                    <Fragment set:html={link.icon} />
                                </a>
                            ))}
                        </div>
                    </div>

                    <!-- Sección de comentarios -->
                    <Comments postSlug={post.slug} />
                </article>

                <!-- Barra lateral -->
                <div class="lg:col-span-1">
                    <BlogSidebar currentSlug={post.slug} currentCategories={post.categories} />
                </div>
            </div>
        </div>
    </main>
</Layout>

<style>
    /* Estilos adicionales para el contenido del post */
    .prose img {
        @apply rounded-lg;
    }
</style>
