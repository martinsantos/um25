---
/**
 * Antecedente Detail Page - V4 Design System
 * Case study/project detail with enhanced design matching V4 template
 */

import LayoutV4 from '../../../layouts/LayoutV4.astro';
import EvidenceCaseRow from '../../../components/um/EvidenceCaseRow.astro';
import UMButton from '../../../components/um/UMButton.astro';
import IntentLinkGraph from '../../../components/um/IntentLinkGraph.astro';
import EmailLink from '../../../components/common/EmailLink.astro';
import { generateSlug } from '../../../utils/slugUtils.js';
import { getServicioById } from '../../../utils/directusHelpers';
import { getServiceIdFromArea } from '../../../data/areaToServiceMap.js';
import { getAntecedenteConServicios, getAntecedenteImageUrl, getDirectusImageUrl, getGeneratedAntecedenteImageUrl, getAllAntecedentes } from '../../../lib/directus.ts';
import { canonicalUrl as toCanonicalUrl, publicImageUrl } from '../../../utils/seoUrl';
import {
  curateAntecedente,
  formatAntecedenteDate,
  formatAntecedenteYear,
} from '../../../utils/antecedentesCuration';
import {
  applyDisplayNameReplacements,
  formatDisplayName,
  plainTextFromHtml,
  renderEditorialBody,
  sanitizeEditorialText,
  sanitizeMarketingCopy,
  truncateEditorialText,
} from '../../../utils/editorialContent';
import { isReplicaIdenticalCopy, resolveReplicaH1 } from '../../../utils/replicaProdCopy';
import {
  buildCaseStrategicLinkGroups,
  buildStrategicWebPageStructuredData,
} from '../../../data/strategicLinkGraph';
import { buildCaseSeoMeta } from '../../../utils/seoMetaPolicy';
import { editorialImages, editorialImageDimensions, generatedAntecedenteImageDimensions } from '../../../data/editorialImageSystem';

// --- Configuration ---
const DEFAULT_IMAGE_URL = `/images/default-background.jpg`;

export const prerender = false;

// URL params
const { id, slug: slugFromUrl } = Astro.params;

if (!id || !slugFromUrl) {
    return Astro.redirect('/404');
}

// --- State ---
let antecedente: any = null;

// --- Helper Functions ---
const getAssetUrl = (assetId: any): string => {
    if (!assetId) return DEFAULT_IMAGE_URL;

    // Handle object with nested ID properties
    if (typeof assetId === 'object') {
        if (assetId.id) return getDirectusImageUrl(assetId.id);
        if (assetId.directus_files_id) return getDirectusImageUrl(assetId.directus_files_id);
    }

    // Handle string UUIDs
    if (typeof assetId === 'string') {
        if (assetId.startsWith('http')) return assetId;
        const url = getDirectusImageUrl(assetId);
        if (url) return url;
    }

    return DEFAULT_IMAGE_URL;
};

// --- Get data and validate: Directus is the only public source for antecedentes. ---
try {
    antecedente = await getAntecedenteConServicios(id);

    if (antecedente) {
        const expectedSlug = generateSlug(antecedente.Titulo);
        if (expectedSlug !== slugFromUrl) {
            return Astro.redirect(`/antecedentes/${id}/${expectedSlug}`, 301);
        }
    } else {
        return Astro.redirect('/404');
    }
} catch (error) {
    console.error(`Error loading antecedente ${id} from Directus:`, error);
    return new Response('Directus antecedente unavailable', {
      status: 503,
      headers: { 'Content-Type': 'text/plain; charset=utf-8' },
    });
}

if (!antecedente) return Astro.redirect('/404');

antecedente = curateAntecedente(antecedente);
const caseRecordLabel = antecedente.curation?.recordLabel || 'Registro técnico';
const caseRecordTone = antecedente.curation?.recordTone || 'technical-record';
const caseRecordSummary = antecedente.curation?.recordSummary || 'Antecedente real cargado desde Directus.';

// --- Prepare data for UI (generated-case image first, Directus as fallback) ---
const directusMainImageUrl = getAssetUrl(antecedente.Imagen || antecedente.ImagenPrincipal);
const generatedMainImageUrl = getGeneratedAntecedenteImageUrl(antecedente.id) || getGeneratedAntecedenteImageUrl(antecedente.original_id);
const mainImageUrl = generatedMainImageUrl
    || (directusMainImageUrl && !directusMainImageUrl.includes('default-background') ? directusMainImageUrl : '')
    || DEFAULT_IMAGE_URL;
const hasGeneratedMainImage = mainImageUrl.startsWith('/images/antecedentes/generated/');
const caseImageDimensions = hasGeneratedMainImage ? generatedAntecedenteImageDimensions : { width: 720, height: 720 };
const caseSeoImageUrl = mainImageUrl.startsWith('/uploads/antecedentes/') ? editorialImages.defaultOg : mainImageUrl;
const caseSeoImageDimensions = hasGeneratedMainImage
  ? generatedAntecedenteImageDimensions
  : mainImageUrl.startsWith('/uploads/antecedentes/')
    ? editorialImageDimensions
    : caseImageDimensions;

const formatDate = (dateString: string): string => {
    const formatted = formatAntecedenteDate(dateString);
    return formatted || '';
};
const caseDateLabel = formatAntecedenteDate(antecedente.Fecha);
const caseDatePublished = caseDateLabel ? antecedente.Fecha : undefined;

const cleanDisplayText = (value: unknown) => String(value || '')
  .replace(/<[^>]+>/g, '')
  .replace(/\s+/g, ' ')
  .trim();
const displayClientName = formatDisplayName(antecedente.Cliente);
const displayCaseText = (value: unknown) => applyDisplayNameReplacements(value, [antecedente.Cliente]);

const caseBodySource = antecedente.displayDescription || antecedente.Descripcion || antecedente.descripcion || '';
const caseBodyPlain = plainTextFromHtml(caseBodySource, 4000);
const caseDescriptionHtml = await renderEditorialBody(caseBodySource);
const rawCaseHeroExcerpt = truncateEditorialText(caseBodySource, 170)
  || cleanDisplayText(antecedente.displayTitle || antecedente.Titulo || antecedente.Nombre);
const caseHeroExcerpt = displayCaseText(rawCaseHeroExcerpt);

const buildStructuredScopeItems = (item: any): string[] => {
  const rows = [
    item.displayTitle || item.Titulo ? `Alcance: ${cleanDisplayText(item.displayTitle || item.Titulo)}` : '',
    item.Cliente ? `Cliente: ${formatDisplayName(item.Cliente)}` : '',
    item.Area ? `Sector: ${cleanDisplayText(item.Area)}` : '',
    item.Unidad_de_negocio ? `Unidad: ${cleanDisplayText(item.Unidad_de_negocio)}` : '',
    formatAntecedenteDate(item.Fecha) ? `Cierre: ${formatDate(item.Fecha)}` : '',
  ].filter((row) => row.length > 8);
  return rows.slice(0, 5);
};

const extractScopeItems = (descripcion: string): string[] => {
  const plain = plainTextFromHtml(descripcion, 2000);
  if (!plain) return [];
  const sentences = plain
    .split(/[.;]/)
    .map((s) => sanitizeEditorialText(s))
    .filter((s) => s.length > 12 && s.length < 160);
  return sentences.slice(0, 5);
};

const scopeFromDescription = extractScopeItems(caseBodySource);
const scopeItems = (() => {
  const bodyNorm = caseBodyPlain.toLowerCase().replace(/\s+/g, ' ').trim();
  const uniqueFromDesc = scopeFromDescription.filter((item) => {
    const snippet = item.toLowerCase().replace(/\s+/g, ' ').trim();
    return snippet.length > 20 && !bodyNorm.includes(snippet);
  });
  if (uniqueFromDesc.length >= 2) return uniqueFromDesc;
  return buildStructuredScopeItems(antecedente);
})();

const isDuplicateCallout = (() => {
  const callout = `Proyecto aplicado en ${antecedente.Area || ''} para ${antecedente.Cliente || ''}`.toLowerCase();
  const bodyStart = caseBodyPlain.slice(0, 180).toLowerCase();
  return bodyStart.includes(callout.slice(0, 40)) || caseBodyPlain.length < 90;
})();

const caseCalloutText = [
  'Proyecto aplicado',
  antecedente.Area ? `en ${cleanDisplayText(antecedente.Area)}` : '',
  displayClientName ? `para ${displayClientName}` : '',
].filter(Boolean).join(' ');

const resolveServiceImageUrl = (servicio: any): string => {
  const serviceImage = getDirectusImageUrl(servicio?.Imagen);
  if (serviceImage) return serviceImage;
  const firstProduct = servicio?.productos?.[0];
  if (firstProduct?.imagen) return firstProduct.imagen;
  if (firstProduct?.Imagen) return getDirectusImageUrl(firstProduct.Imagen) || '';
  return '/images/default-background.jpg';
};

const compactServiceTitle = (value: unknown): string => {
  const title = cleanDisplayText(value)
    .replace(/\s*\|.*$/g, '')
    .trim();
  const replacements: Record<string, string> = {
    'Infraestructura de Redes': 'Redes e infraestructura',
    'Sistemas de Seguridad Electrónica': 'Seguridad electrónica',
    'Telecomunicaciones.': 'Telecomunicaciones',
    'Desarrollo de Software a Medida': 'Software operativo',
    'Soporte Técnico 24/7': 'Soporte técnico 24/7',
    'Consultoría IT y Transformación Digital': 'Consultoría IT',
    'Sistemas de Detección y Alarma de Incendios': 'Detección de incendios',
    'Servicios Eléctricos para IT': 'Energía para IT',
  };
  return replacements[title] || title || 'Servicio IT';
};

// Get related services via M2M relationship from Directus (with fallback to area mapping)
const getRelatedServices = async (antecedenteId: string, area: string) => {
    // TODO: Re-enable M2M relationship when function is available in directusHelpers.ts
    // try {
    //     // Try to get M2M related services from Directus
    //     const serviciosFromDirectus = await getServiciosPorAntecedente(parseInt(antecedenteId));
    //
    //     if (serviciosFromDirectus && serviciosFromDirectus.length > 0) {
    //         return serviciosFromDirectus.slice(0, 4); // Max 4 services
    //     }
    // } catch (error) {
    //     console.error('Error fetching related services from Directus:', error);
    // }

    // Fallback: Use area-based mapping to find one service from Directus
    try {
        const serviceId = getServiceIdFromArea(area);
        const servicio = await getServicioById(serviceId);

        if (servicio) {
            const lead = sanitizeMarketingCopy(
              servicio.Subtitulo || servicio.Descripcion,
              280,
            );
            return [{
                id: serviceId,
                Titulo: servicio.Titulo,
                Descripcion: servicio.Descripcion,
                lead,
                imageUrl: resolveServiceImageUrl(servicio),
                imageAlt: compactServiceTitle(servicio.Titulo),
                slug: generateSlug(servicio.Titulo),
            }];
        }
    } catch (error) {
        console.error('Error fetching service by area mapping:', error);
    }

    return [];
};

const relatedServices = await getRelatedServices(id, antecedente.Area || '');

const getRelatedAntecedentes = async (area: string, excludeId: string, limit: number = 3) => {
  try {
    const all = await getAllAntecedentes();
    return all
      .filter(item => {
        if (String(item.id) === excludeId) return false;
        const itemArea = (item.Area || '').toLowerCase();
        const searchArea = (area || '').toLowerCase();
        return itemArea.includes(searchArea) || searchArea.includes(itemArea);
      })
      .slice(0, limit)
      .map(item => ({
        ...curateAntecedente(item),
        slug: generateSlug(item.Titulo || 'proyecto'),
        imageUrl: getAntecedenteImageUrl(item)
      }));
  } catch {
    return [];
  }
};

const relatedAntecedentes = await getRelatedAntecedentes(antecedente.Area || '', id);

const buildCaseHeadline = (item: any): string => {
  const title = cleanDisplayText(item.Titulo || item.Nombre || 'Proyecto técnico documentado');
  const client = formatDisplayName(item.Cliente);
  const area = cleanDisplayText(item.Area);
  const normalized = title.toLowerCase();

  if (normalized.includes('digitalización') || normalized.includes('digitalizacion') || normalized.includes('software')) {
    if (client) return `Software operativo para ${client}`;
    return 'Software operativo documentado';
  }
  if (normalized.includes('soporte')) {
    if (client) return `Soporte IT documentado para ${client}`;
    return 'Soporte IT documentado';
  }
  if (normalized.includes('cctv') || normalized.includes('cámara') || normalized.includes('camara')) {
    if (client) return `CCTV operativo para ${client}`;
    return 'CCTV operativo documentado';
  }
  if (normalized.includes('detección') || normalized.includes('deteccion') || normalized.includes('incendio')) {
    if (client) return `Detección y seguridad para ${client}`;
    return 'Detección y seguridad documentada';
  }
  if (normalized.includes('redes') || normalized.includes('fibra')) {
    if (client) return `Redes y fibra para ${client}`;
    return 'Redes y fibra documentadas';
  }
  if (normalized.includes('eléctrica') || normalized.includes('electrica') || normalized.includes('data center')) {
    if (client) return `Energía IT para ${client}`;
    return 'Energía IT documentada';
  }

  const beforeDash = title.split(/\s[-–]\s/)[0]?.trim() || title;
  if (beforeDash.length <= 74) return beforeDash;
  if (client && area) return `${area} para ${client}`.slice(0, 82);
  if (client) return `Proyecto técnico para ${client}`.slice(0, 82);
  return `${beforeDash.slice(0, 72).trimEnd()}…`;
};

const caseHeadlineSource = isReplicaIdenticalCopy()
  ? resolveReplicaH1(Astro.url.pathname, cleanDisplayText(antecedente.displayTitle || antecedente.Titulo || antecedente.Nombre))
  : cleanDisplayText(antecedente.displayTitle || antecedente.Titulo || antecedente.Nombre);
const baseCaseHeadline = buildCaseHeadline({ ...antecedente, Titulo: caseHeadlineSource });
const caseHeadline = (() => {
  if (antecedente.curation?.quality === 'low-value-candidate') {
    return `Registro documental: ${baseCaseHeadline}`;
  }
  if (antecedente.curation?.quality === 'data-error-candidate') {
    return `Registro técnico en revisión: ${baseCaseHeadline}`;
  }
  return baseCaseHeadline;
})();
const compactCaseTitle = (value: unknown, client?: unknown): string => {
  const title = cleanDisplayText(value)
    .replace(/Sistena/gi, 'Sistema')
    .replace(/\s+\(\d+\)$/g, '')
    .trim();
  const organization = formatDisplayName(client);
  if (!title) return organization || 'Proyecto técnico';

  const parts = title.split(/\s[-–]\s/).map((part) => part.trim()).filter(Boolean);
  let base = parts[0] || title;
  base = base
    .replace(/Soporte de Infraestructura IT de Alta Disponibilidad/gi, 'Soporte IT de alta disponibilidad')
    .replace(/Implementación de Redes de Datos y Fibra Óptica/gi, 'Redes y fibra óptica')
    .replace(/Instalación eléctrica para Data Center/gi, 'Energía para data center')
    .replace(/Sistema Detección de Incendios/gi, 'Detección de incendios');

  const target = parts[1] && !/^\d{4}|abril|mayo|junio|julio|agosto|septiembre|octubre|noviembre|diciembre/i.test(parts[1])
    ? parts[1]
    : organization;
  const composed = target ? `${base}: ${target}` : base;
  return composed.length > 78 ? `${composed.slice(0, 75).trimEnd()}...` : composed;
};

// SEO
const siteUrl = 'https://www.ultimamilla.com.ar';
const canonicalUrl = toCanonicalUrl(`/antecedentes/${id}/${slugFromUrl}`);
const caseSeo = buildCaseSeoMeta({
  title: antecedente.displayTitle || antecedente.Titulo || antecedente.Nombre,
  description: antecedente.displayDescription || caseBodySource,
  area: antecedente.Area || antecedente.Unidad_de_negocio,
  date: antecedente.Fecha,
});
const metaDescription = caseSeo.description;
const structuredImageUrl = publicImageUrl(caseSeoImageUrl) || toCanonicalUrl(DEFAULT_IMAGE_URL);
const caseDossierFacts = [
  { label: 'Registro', value: `ANT-${id}` },
  { label: 'Tipo', value: caseRecordLabel },
  displayClientName ? { label: 'Organización', value: displayClientName } : null,
  antecedente.Area ? { label: 'Vertical', value: cleanDisplayText(antecedente.Area) } : null,
  caseDateLabel ? { label: 'Fecha', value: caseDateLabel } : null,
].filter(Boolean) as Array<{ label: string; value: string }>;
const caseStrategicLinkGroups = buildCaseStrategicLinkGroups({
  currentPath: `/antecedentes/${id}/${slugFromUrl}`,
  title: caseHeadline,
  summary: metaDescription,
  sectorName: antecedente.Area || antecedente.Unidad_de_negocio,
  text: `${caseBodyPlain} ${antecedente.Cliente || ''}`,
  relatedServices,
  relatedCases: relatedAntecedentes,
});
const antecedenteStructuredData = {
  "@type": "CreativeWork",
  "name": antecedente.displayTitle || antecedente.Titulo,
  "headline": antecedente.displayTitle || antecedente.Titulo,
  "description": metaDescription,
  "url": canonicalUrl,
  "image": {
    "@type": "ImageObject",
    "url": structuredImageUrl,
    "caption": [
      antecedente.displayTitle || antecedente.Titulo,
      antecedente.Cliente ? `Cliente: ${cleanDisplayText(antecedente.Cliente)}` : '',
      antecedente.Area ? `Sector: ${cleanDisplayText(antecedente.Area)}` : '',
    ].filter(Boolean).join(' | '),
  },
  "datePublished": caseDatePublished,
  "inLanguage": "es-AR",
  "genre": caseRecordLabel,
  "about": antecedente.Area ? { "@type": "Thing", "name": cleanDisplayText(antecedente.Area) } : undefined,
  "mentions": antecedente.Cliente ? [{ "@type": "Organization", "name": cleanDisplayText(antecedente.Cliente) }] : undefined,
  "identifier": `ANT-${id}`,
  "isPartOf": {
    "@type": "CollectionPage",
    "name": "Evidencia operativa documentada",
    "url": toCanonicalUrl('/antecedentes')
  },
  "provider": {
    "@type": "Organization",
    "name": "ULTIMA MILLA",
    "url": siteUrl
  }
};
const antecedentePageStructuredData = [
  antecedenteStructuredData,
  buildStrategicWebPageStructuredData({
    url: canonicalUrl,
    name: `${antecedente.Titulo} | ULTIMA MILLA`,
    description: metaDescription,
    groups: caseStrategicLinkGroups,
  }),
];
---

<LayoutV4
  title={caseSeo.title}
  description={metaDescription}
  image={caseSeoImageUrl}
  imageWidth={caseSeoImageDimensions.width}
  imageHeight={caseSeoImageDimensions.height}
  canonical={canonicalUrl}
  structuredData={antecedentePageStructuredData}
>
  <section class="case-detail-hero">
    <div class="case-detail-hero__wash"></div>

    <div class="um-container case-detail-hero__content">
      <nav class="case-detail-breadcrumb" aria-label="Breadcrumb">
        <a href="/">Inicio</a>
        <span>/</span>
        <a href="/antecedentes">Antecedentes</a>
        <span>/</span>
        <span>{antecedente.Area || 'Proyecto'}</span>
      </nav>

      <div class="case-detail-hero__grid">
        <div class="case-detail-hero__copy">
          <div class:list={['case-detail-label', `is-${caseRecordTone}`]}>
            <span>{antecedente.Area || 'Proyecto'}</span>
          </div>

          <h1>{caseHeadline}</h1>

          <p>
            {caseHeroExcerpt}
          </p>

          <div class="case-detail-actions">
            <UMButton href="/contacto">Solicitar relevamiento</UMButton>
            <UMButton href="/antecedentes" variant="secondary">Volver al archivo</UMButton>
          </div>
        </div>

        <aside class="case-detail-dossier" aria-label="Dossier documental del antecedente">
          <figure class="case-detail-dossier__media">
            <img
              src={mainImageUrl}
              alt={antecedente.Titulo}
              loading="eager"
              fetchpriority="high"
              decoding="async"
              width={caseImageDimensions.width}
              height={caseImageDimensions.height}
              onerror="this.src='/images/default-background.jpg'"
            />
          </figure>
          <div class="case-detail-dossier__body">
            <span>{caseRecordLabel}</span>
            <strong>{displayClientName || `ANT-${id}`}</strong>
            <p>{caseRecordSummary}</p>
            <div class="case-detail-meta">
              {antecedente.Area && (
                <div>
                  <span>Vertical</span>
                  <strong>{antecedente.Area}</strong>
                </div>
              )}
              {caseDateLabel && (
                <div>
                  <span>Fecha</span>
                  <strong>{caseDateLabel}</strong>
                </div>
              )}
              <div>
                <span>Registro</span>
                <strong>ANT-{id}</strong>
              </div>
            </div>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <section class="case-proof-strip" aria-label="Ficha rápida del antecedente">
    <div class="um-container case-proof-strip__grid">
      {caseDossierFacts.map((fact) => (
        <div>
          <span>{fact.label}</span>
          <strong>{fact.value}</strong>
        </div>
      ))}
    </div>
  </section>

  <!-- CONTENT SECTION -->
  <section class="case-detail-main case-detail">
    <div class="um-container case-detail-main__grid">
      <!-- Left Column (Main Content) -->
      <div class="case-detail-body">

        <!-- Description Section -->
        <div class="flex flex-col gap-4 sm:gap-5">
          <div class="case-section-title">
            <span aria-hidden="true"></span>
            <h2>Lectura del antecedente</h2>
          </div>
          <div class="case-detail-body__copy editorial-body">
            {caseDescriptionHtml ? (
              <div set:html={caseDescriptionHtml} />
            ) : (
              <p>{caseHeroExcerpt}</p>
            )}
          </div>

          {!isDuplicateCallout && caseCalloutText.length > 'Proyecto aplicado'.length && (
            <div class="case-callout">
              <span>{caseCalloutText}</span>
            </div>
          )}
        </div>

        <!-- Alcance del Proyecto Section -->
        {scopeItems.length > 0 && (
          <div class="flex flex-col gap-4 sm:gap-5">
            <div class="case-section-title">
              <span aria-hidden="true"></span>
              <h2>Alcance documentado</h2>
            </div>
            <ul class="case-scope-list">
              {scopeItems.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
        )}

        <!-- Servicios Relacionados -->
        {relatedServices.length > 0 && (
          <div class="flex flex-col gap-4 sm:gap-5">
            <div class="case-section-title">
              <span aria-hidden="true"></span>
              <h2>Servicios aplicados</h2>
            </div>
            <ul class="case-service-cards">
              {relatedServices.map((servicio, serviceIndex) => (
                <li>
                  <a class="um-click-surface case-service-card" href={`/servicios/${servicio.id}/${servicio.slug}`}>
                    <figure class="case-service-card__media">
                      <img
                        src={servicio.imageUrl || '/images/default-background.jpg'}
                        alt={servicio.imageAlt}
                        loading={serviceIndex === 0 ? 'eager' : 'lazy'}
                        decoding="async"
                        width="320"
                        height="240"
                        onerror="this.onerror=null;this.src='/images/default-background.jpg'"
                      />
                    </figure>
                    <div class="case-service-card__body">
                      <h3>{compactServiceTitle(servicio.Titulo)}</h3>
                      <p>{servicio.lead}</p>
                      <span class="case-service-card__cta">Ver servicio</span>
                    </div>
                  </a>
                </li>
              ))}
            </ul>

            <a href="/servicios" class="um-click-surface case-service-cards__all">Ver todos los servicios</a>
          </div>
        )}

        <aside class="case-source-note" aria-label="Fuente documental">
          <span>Fuente documental</span>
          <p>
            Datos del antecedente cargados en Directus. Imagen única asociada al registro público ANT-{id}.
          </p>
        </aside>

        <!-- Gallery (if has additional images from Directus) -->
        {antecedente.Imagenes && antecedente.Imagenes.length > 0 && (
          <div class="flex flex-col gap-4 sm:gap-5">
            <div class="case-section-title">
              <span aria-hidden="true"></span>
              <h2>Galería del proyecto</h2>
            </div>
            <div class="case-gallery-grid">
              {antecedente.Imagenes.map((img: any) => (
                <figure class="case-gallery-frame">
                  <img
                    src={getAssetUrl(img)}
                    alt="Galería del proyecto"
                    class="case-gallery-image"
                    loading="lazy"
                    onerror="this.src='/images/default-background.jpg'"
                  />
                </figure>
              ))}
            </div>
          </div>
        )}
      </div>

      <!-- Sidebar -->
      <div class="case-detail-side">

        <!-- Ficha Técnica Card -->
        <div class="case-info-card">
          <div class="case-info-card__head">
            <span class="case-info-card__icon" aria-hidden="true">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
              </svg>
            </span>
            <span class="case-info-card__title">Ficha técnica</span>
          </div>
          <div class="p-0 flex flex-col gap-3 sm:gap-4">
            {antecedente.Cliente && (
              <>
                <div class="flex flex-col gap-1">
                  <span class="case-info-label">Organización</span>
                  <span class="case-info-value">{displayClientName}</span>
                </div>
                <div class="h-px bg-gray-100"></div>
              </>
            )}
            {antecedente.Area && (
              <>
                <div class="flex flex-col gap-1">
                  <span class="case-info-label">Vertical</span>
                  <span class="case-info-value">{antecedente.Area}</span>
                </div>
                <div class="h-px bg-gray-100"></div>
              </>
            )}
            {antecedente.Unidad_de_negocio && (
              <>
                <div class="flex flex-col gap-1">
                  <span class="case-info-label">Unidad de Negocio</span>
                  <span class="case-info-value">{antecedente.Unidad_de_negocio}</span>
                </div>
                <div class="h-px bg-gray-100"></div>
              </>
            )}
            {caseDateLabel && (
              <>
                <div class="flex flex-col gap-1">
                  <span class="case-info-label">Finalización</span>
                  <span class="case-info-value">{caseDateLabel}</span>
                </div>
              </>
            )}
            <div class="h-px bg-gray-100"></div>
            <div class="flex flex-col gap-1">
              <span class="case-info-label">Jerarquía</span>
              <span class="case-info-value">{caseRecordLabel}</span>
            </div>
            <div class="h-px bg-gray-100"></div>
            <div class="flex flex-col gap-1">
              <span class="case-info-label">Código Proyecto</span>
              <span class="case-info-value case-info-value--code">ANT-{id}</span>
            </div>
          </div>
        </div>

        <!-- CTA Widget -->
        <div class="case-cta-card">
          <div class="case-cta-card__icon">
            <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
          </div>
          <h3>Relevar una operación similar</h3>
          <p>
            Podemos relevar contexto, riesgos, alcance y próximos pasos verificables.
          </p>
          <div class="case-cta-card__actions">
            <a href="/contacto">
              <span class="case-cta-card__action-icon" aria-hidden="true">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                </svg>
              </span>
              Solicitar relevamiento
            </a>
            <EmailLink label="Enviar email">
              <span class="case-cta-card__action-icon" aria-hidden="true">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                </svg>
              </span>
              Enviar email
            </EmailLink>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- RELATED SECTION -->
  {relatedAntecedentes.length > 0 && (
    <section class="case-related">
      <div class="um-container">
        <div class="case-related__head">
          <div>
            <span class="um-kicker">Archivo relacionado</span>
            <h2>Evidencia relacionada</h2>
          </div>
          <a href="/antecedentes" class="um-click-surface case-related__link">
            Ver antecedentes
            <span class="case-related__icon" aria-hidden="true">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
              </svg>
            </span>
          </a>
        </div>

        <div class="case-related__ledger">
          {relatedAntecedentes.map((ant, index) => (
            <EvidenceCaseRow
              href={`/antecedentes/${ant.id}/${ant.slug}`}
              number={String(index + 1).padStart(2, '0')}
              title={compactCaseTitle(ant.displayTitle || ant.Titulo || 'Proyecto', ant.Cliente)}
              description={applyDisplayNameReplacements(
                truncateEditorialText(ant.displayDescription || ant.Descripcion || ant.Titulo, 150),
                [ant.Cliente]
              )}
              image={ant.imageUrl}
              imageAlt={ant.Titulo || 'Antecedente'}
              sector={ant.Area || 'Servicios IT'}
              year={formatAntecedenteYear(ant.Fecha)}
              imageLoading="eager"
            />
          ))}
        </div>

        <div class="case-related__footer">
          <a href="/antecedentes" class="um-click-surface">
            Explorar archivo completo
            <span class="case-related__icon case-related__icon--lg" aria-hidden="true">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
              </svg>
            </span>
          </a>
        </div>
      </div>
    </section>
  )}

  <IntentLinkGraph
    kicker="Para avanzar"
    title="Ver también"
    text="Servicio aplicado, casos comparables y contacto para pedir un relevamiento similar."
    groups={caseStrategicLinkGroups}
  />

  <section class="case-detail-footer-cta">
    <div class="um-container case-detail-footer-cta__grid">
      <div>
        <span class="um-kicker">Siguiente paso</span>
        <h2>Convertir esta evidencia en un alcance verificable</h2>
        <p>Relevamos sitio, riesgo, dependencias y prioridad técnica antes de proponer una intervención.</p>
      </div>
      <div class="case-detail-footer-cta__actions">
        <UMButton href="/contacto">Solicitar relevamiento</UMButton>
        <UMButton href="/antecedentes" variant="secondary">Ver archivo completo</UMButton>
      </div>
    </div>
  </section>
</LayoutV4>

<style>
  /* Escala editorial única — detalle antecedente (cuerpo 18px, sección ~1.11×) */
  .case-detail-hero,
  .case-proof-strip,
  .case-detail-main.case-detail,
  .case-related,
  .case-detail-footer-cta {
    --case-body: 1.125rem;
    --case-body-line: 1.72;
    --case-section: clamp(1.2rem, 1.15vw, 1.3125rem);
    --case-hero-excerpt: 1.1875rem;
    --case-meta-label: 1rem;
    --case-meta-value: 1rem;
    --case-sidebar-label: 1rem;
    --case-sidebar-value: 1.0625rem;
    --case-card-title: 1.0625rem;
  }

  .case-detail-hero {
    position: relative;
    overflow: hidden;
    min-height: 0;
    border-bottom: 0;
    background: #fff;
    color: #111;
  }

  .case-detail-hero__wash {
    position: absolute;
    inset: 0;
  }

  .case-detail-hero__wash {
    background:
      linear-gradient(90deg, #fff 0%, rgba(255,255,255,0.98) 52%, rgba(255,255,255,0.9) 100%),
      linear-gradient(135deg, rgba(220,38,38,0.08) 0 1px, transparent 1px 32px),
      linear-gradient(0deg, #fff 0%, rgba(255,255,255,0.72) 72%);
  }

  .case-detail-hero__content {
    position: relative;
    z-index: 1;
    padding-top: clamp(22px, 2.6vw, 36px);
    padding-bottom: clamp(26px, 4vw, 48px);
  }

  .case-detail-breadcrumb {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin-bottom: clamp(18px, 2.8vw, 30px);
    color: #5f6368;
    font-size: 1rem;
    font-weight: 700;
  }

  .case-detail-breadcrumb a:hover {
    color: #111;
  }

  .case-detail-hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.58fr) minmax(340px, 0.42fr);
    gap: clamp(26px, 4.2vw, 56px);
    align-items: start;
  }

  .case-detail-hero__grid > *,
  .case-detail-hero__copy {
    min-width: 0;
  }

  .case-detail-label {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    margin-bottom: 0.85rem;
    padding: 0 !important;
    border-radius: 0 !important;
    background: transparent !important;
    color: #DC2626 !important;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0;
    line-height: 1.2;
  }

  .case-detail-label::before {
    content: "";
    width: 34px;
    height: 2px;
    background: #DC2626;
  }

  .case-detail-label.is-documentary-record,
  .case-detail-label.is-technical-record {
    color: #4b5563 !important;
  }

  .case-detail-label.is-documentary-record::before,
  .case-detail-label.is-technical-record::before {
    background: #6b7280;
  }

  .case-detail-label.is-editorial-review {
    color: #92400e !important;
  }

  .case-detail-label.is-editorial-review::before {
    background: #92400e;
  }

  .case-detail-hero__copy h1 {
    max-width: min(100%, 20ch);
    color: #111;
    font-size: clamp(2.5rem, 2.45vw, 2.75rem);
    line-height: 1.1;
    font-weight: var(--um-weight-title, 600);
    letter-spacing: 0;
    text-wrap: balance;
    overflow-wrap: anywhere;
  }

  .case-detail-hero__copy > p {
    max-width: 650px;
    margin-top: 1rem;
    color: #535a62;
    font-size: var(--case-hero-excerpt);
    line-height: 1.65;
  }

  .case-detail-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: clamp(18px, 2.5vw, 28px);
  }

  .case-detail-actions :global(.um-btn) {
    min-width: 216px;
  }

  .case-detail-actions :global(.um-btn-secondary) {
    background: #f1f3f5;
    color: #111;
  }

  .case-detail-actions :global(.um-btn-secondary:hover) {
    background: #111;
    color: #fff;
  }

  .case-detail-dossier {
    overflow: hidden;
    width: min(100%, 480px);
    justify-self: end;
    border: 0;
    background: #fff;
    box-shadow: none;
  }

  .case-detail-dossier__media {
    aspect-ratio: 16 / 9;
    width: 100%;
    max-width: 100%;
    margin: 0;
    min-height: 190px;
    overflow: hidden;
    background: #111;
  }

  .case-detail-dossier__media img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    filter: saturate(0.92) contrast(1.05);
  }

  .case-detail-dossier__body {
    padding: clamp(16px, 1.8vw, 20px) clamp(16px, 1.9vw, 22px) clamp(17px, 2vw, 22px);
  }

  .case-detail-dossier__body > span {
    display: block;
    color: #dc2626 !important;
    font-size: 1rem;
    font-weight: 700;
    line-height: 1.3;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .case-detail-dossier__body > strong {
    display: block;
    margin-top: 0.65rem;
    color: #111 !important;
    font-family: var(--um-font-display);
    font-size: clamp(1.28rem, 1.65vw, 1.55rem);
    font-weight: var(--um-weight-title, 600);
    line-height: 1.12;
    text-wrap: balance;
  }

  .case-detail-dossier__body > p {
    margin: 0.75rem 0 0;
    color: #4b5563;
    font-size: 1rem;
    line-height: 1.58;
  }

  .case-detail-meta {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem 1.25rem;
    margin-top: 1.1rem;
    padding: 0.9rem;
    border-top: 0;
    background: #f7f8f9;
  }

  .case-detail-meta div {
    min-width: 0;
    padding: 0;
    background: transparent;
  }

  .case-detail-dossier .case-detail-meta span {
    display: block;
    color: #6b7280 !important;
    font-size: var(--case-meta-label);
    font-weight: 700;
    line-height: 1.25;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .case-detail-dossier .case-detail-meta strong {
    display: block;
    margin-top: 0.35rem;
    color: #111 !important;
    font-size: var(--case-meta-value);
    font-weight: 600;
    line-height: 1.35;
    overflow-wrap: anywhere;
    text-transform: none;
  }

  .case-proof-strip {
    background: #f5f5f5;
    color: #111;
    border-top: 1px solid #e4e6e9;
    border-bottom: 1px solid #e4e6e9;
  }

  .case-proof-strip__grid {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: clamp(10px, 1.4vw, 16px);
    background: transparent;
    padding-block: clamp(14px, 2vw, 20px);
  }

  .case-proof-strip__grid div {
    min-width: 0;
    border-left: 3px solid #DC2626;
    background: #fff;
    padding: clamp(14px, 1.6vw, 18px) clamp(14px, 1.8vw, 20px);
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.04);
  }

  .case-proof-strip__grid span {
    display: block;
    color: #5f6670;
    font-size: 1rem;
    font-weight: 700;
    line-height: 1.25;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .case-proof-strip__grid strong {
    display: block;
    margin-top: 8px;
    color: #111;
    font-size: clamp(1.02rem, 1.12vw, 1.16rem);
    font-weight: 700;
    line-height: 1.35;
    overflow-wrap: anywhere;
  }

  .case-detail-main {
    padding: clamp(42px, 5.5vw, 72px) 0;
    background: #fff;
  }

  .case-detail-main__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.68fr) minmax(300px, 0.32fr);
    gap: clamp(34px, 6vw, 72px);
    align-items: start;
  }

  .case-detail-main__grid > * {
    min-width: 0;
  }

  .case-detail-body {
    display: grid;
    gap: clamp(30px, 4.5vw, 48px);
    max-width: 820px;
  }

  .case-detail-side {
    display: grid;
    gap: 1rem;
    position: sticky;
    top: 104px;
    z-index: 1;
    align-self: start;
  }

  .case-section-title {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    margin-bottom: 1rem;
  }

  .case-section-title > span {
    flex: 0 0 34px;
    width: 34px;
    height: 2px;
    background: #DC2626;
  }

  .case-section-title h2 {
    color: #111;
    font-size: var(--case-section);
    line-height: 1.2;
    font-weight: var(--um-weight-title, 600);
    letter-spacing: 0;
    text-wrap: balance;
  }

  .case-detail-body__copy {
    color: #1f2937;
    font-size: var(--case-body);
    line-height: var(--case-body-line);
  }

  .case-detail-body__copy :global(p),
  .case-detail-body__copy :global(li) {
    font-size: inherit;
    line-height: 1.75;
    color: #374151;
  }

  .case-detail-body__copy :global(p[style]),
  .case-detail-body__copy :global(li[style]),
  .case-detail-body__copy :global(span[style]) {
    font-size: inherit !important;
  }

  .case-callout {
    padding: 1rem 1.1rem;
    border-left: 0;
    background: linear-gradient(90deg, #DC2626 0 3px, #FFFFFF 3px);
    color: #111;
    font-size: var(--case-body);
    font-weight: 600;
    line-height: 1.45;
  }

  .case-source-note {
    border-left: 3px solid #DC2626;
    background: #f7f8f9;
    padding: 1.1rem 1.2rem;
  }

  .case-source-note span {
    display: block;
    color: #111;
    font-size: 1rem;
    font-weight: 700;
    line-height: 1.3;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .case-source-note p {
    margin: 0.55rem 0 0;
    color: #4b5563;
    font-size: var(--case-body);
    line-height: 1.62;
  }

  .case-scope-list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 10px;
    border: 0;
    background: transparent;
  }

  .case-scope-list li {
    position: relative;
    padding: 1rem 1.1rem 1rem 2.2rem;
    background: #f7f8f9;
    color: #1f2937;
    font-size: var(--case-body);
    line-height: var(--case-body-line);
    font-weight: 500;
  }

  .case-scope-list li::before {
    content: '';
    position: absolute;
    left: 1.1rem;
    top: 1.35rem;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #DC2626;
  }

  .case-gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
  }

  .case-gallery-frame {
    margin: 0;
    min-height: 260px;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: #f3f4f6;
    overflow: hidden;
  }

  .case-gallery-image {
    width: 100%;
    height: 100%;
    min-height: 260px;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .case-info-card {
    overflow: hidden;
    border-top: 0;
    border-bottom: 0;
    border-radius: 0;
    background: #fff;
  }

  .case-info-card__head {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    padding: 1.1rem 0;
    border-bottom: 0;
    background: #fff;
    color: #111;
  }

  .case-info-card__icon {
    display: inline-grid;
    flex: 0 0 42px;
    width: 42px;
    height: 42px;
    place-items: center;
    color: #dc2626;
    background: rgba(220, 38, 38, 0.08);
  }

  .case-info-card__icon svg {
    display: block;
    width: 24px;
    height: 24px;
    flex-shrink: 0;
  }

  .case-info-card__title {
    color: #111 !important;
    font-size: 1.1875rem;
    font-weight: 700;
    line-height: 1.2;
  }

  .case-info-card :global(.p-4),
  .case-info-card :global(.sm\:p-5) {
    padding: 1.05rem 1rem !important;
  }

  .case-info-card :global(.tracking-wider) {
    letter-spacing: 0 !important;
  }

  .case-info-card :global(.h-px) {
    display: none !important;
  }

  .case-info-label {
    display: block;
    color: #4b5563 !important;
    font-size: var(--case-sidebar-label);
    font-weight: 700;
    line-height: 1.3;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .case-info-value {
    display: block;
    margin-top: 0.35rem;
    color: #111 !important;
    font-size: var(--case-sidebar-value);
    font-weight: 600;
    line-height: 1.4;
  }

  .case-info-value--code {
    color: #dc2626;
    font-family: ui-monospace, monospace;
  }

  .case-cta-card {
    padding: 1.35rem;
    border: 0;
    border-radius: 0;
    background: #080808;
    color: #fff;
  }

  .case-cta-card__icon {
    display: grid;
    width: 48px;
    height: 48px;
    margin-bottom: 1rem;
    place-items: center;
    border: 0;
    color: #dc2626;
    background: rgba(220, 38, 38, 0.12);
  }

  .case-cta-card__icon svg {
    display: block;
    width: 26px;
    height: 26px;
    flex-shrink: 0;
  }

  .case-cta-card__action-icon {
    display: inline-grid;
    flex: 0 0 22px;
    width: 22px;
    height: 22px;
    place-items: center;
  }

  .case-cta-card__action-icon svg {
    display: block;
    width: 20px;
    height: 20px;
    flex-shrink: 0;
  }

  .case-cta-card h3 {
    color: #fff !important;
    font-size: 1.28rem;
    line-height: 1.22;
    font-weight: var(--um-weight-title, 600);
  }

  .case-cta-card p {
    margin-top: 0.75rem;
    color: rgba(255, 255, 255, 0.9) !important;
    font-size: 1.125rem;
    line-height: 1.62;
  }

  .case-cta-card__actions {
    display: grid;
    gap: 0.65rem;
    margin-top: 1.1rem;
  }

  .case-cta-card__actions a {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    min-height: 46px;
    border: 0;
    color: #fff;
    font-size: 1rem;
    font-weight: 700;
    background: rgba(255,255,255,0.03);
  }

  .case-cta-card__actions a:first-child {
    background: #DC2626;
  }

  .case-cta-card__actions a:hover {
    background: #fff;
    color: #080808;
  }

  .case-related {
    position: relative;
    z-index: 2;
    padding: clamp(42px, 5.5vw, 72px) 0;
    border-top: 0;
    background: #FFFFFF;
    scroll-margin-top: 96px;
  }

  .case-related__head {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    justify-content: space-between;
    gap: 1.25rem;
    margin-bottom: clamp(28px, 4vw, 44px);
  }

  .case-related__head h2 {
    margin-top: 1rem;
    color: #111;
    font-size: var(--case-section);
    line-height: 1.2;
    font-weight: var(--um-weight-title, 600);
    text-wrap: balance;
  }

  .case-related__link,
  .case-related__footer a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    min-height: 46px;
    color: #111;
    font-size: 1rem;
    font-weight: 700;
  }

  .case-related__icon {
    display: inline-grid;
    flex: 0 0 20px;
    width: 20px;
    height: 20px;
    place-items: center;
    color: #dc2626;
  }

  .case-related__icon svg {
    display: block;
    width: 18px;
    height: 18px;
    flex-shrink: 0;
  }

  .case-related__icon--lg,
  .case-related__icon--lg svg {
    width: 22px;
    height: 22px;
  }

  .case-related__ledger {
    border-top: 0;
    overflow: hidden;
    display: grid;
    gap: 0.85rem;
  }

  .case-related__ledger :global(.evidence-case-row) {
    isolation: isolate;
  }

  .case-related__ledger :global(.evidence-case-row__thumb) {
    width: 100%;
    height: auto;
    min-width: 156px;
    min-height: 112px;
    max-width: none;
    aspect-ratio: 16 / 10;
  }

  .case-related__ledger :global(.evidence-case-row__copy) {
    padding-left: 0;
  }

  .case-service-cards {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .case-service-card {
    display: grid;
    grid-template-columns: minmax(148px, 200px) minmax(0, 1fr);
    gap: 0;
    border: 0;
    border-top: 0;
    background: #f7f8f9;
    color: inherit;
    text-decoration: none;
    overflow: hidden;
    transition: background-color 180ms ease;
  }

  .case-service-card:hover {
    background: #f1f3f5;
  }

  .case-service-card__media {
    margin: 0;
    min-height: 148px;
    background: #eceff2;
    overflow: hidden;
  }

  .case-service-card__media img {
    display: block;
    width: 100%;
    height: 100%;
    min-height: 148px;
    object-fit: cover;
    object-position: center;
  }

  .case-service-card__body {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.55rem;
    padding: 1.1rem 1.15rem;
  }

  .case-service-card__body h3 {
    margin: 0;
    color: #111;
    font-family: var(--um-font-display);
    font-size: var(--case-card-title);
    font-weight: 600;
    line-height: 1.28;
  }

  .case-service-card__body p {
    margin: 0;
    color: #4b5563;
    font-size: 1rem;
    line-height: 1.62;
  }

  .case-service-card__cta {
    margin-top: 0.2rem;
    color: var(--um-red);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  .case-service-cards__all {
    display: inline-block;
    margin-top: 1rem;
    color: var(--um-red);
    font-size: 1.125rem;
    font-weight: 700;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  @media (max-width: 640px) {
    .case-service-card {
      grid-template-columns: 1fr;
    }

    .case-service-card__media {
      aspect-ratio: 16 / 9;
      min-height: 0;
    }

    .case-service-card__media img {
      min-height: 0;
    }
  }

  .case-detail-footer-cta {
    background: #fff;
    border-top: 0;
    padding: clamp(48px, 6vw, 88px) 0;
  }

  .case-detail-footer-cta__grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: clamp(24px, 4vw, 48px);
    align-items: end;
  }

  .case-detail-footer-cta h2 {
    margin-top: 0.75rem;
    color: #111;
    font-size: var(--case-section);
    font-weight: 600;
    line-height: 1.2;
  }

  .case-detail-footer-cta p {
    max-width: 620px;
    margin-top: 0.85rem;
    color: #5f6368;
    font-size: var(--case-body);
    line-height: 1.65;
  }

  .case-detail-footer-cta__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: flex-end;
  }

  .case-related__footer {
    margin-top: 1.4rem;
  }

  @media (max-width: 1120px) {
    .case-detail-hero__grid,
    .case-detail-main__grid,
    .case-detail-footer-cta__grid {
      grid-template-columns: 1fr;
    }

    .case-detail-footer-cta__actions {
      justify-content: flex-start;
    }

    .case-detail-dossier {
      justify-self: start;
      width: 100%;
      max-width: 760px;
    }

    .case-detail-side {
      position: static;
    }

    .case-proof-strip__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .case-detail-hero,
    .case-proof-strip,
    .case-detail-main.case-detail,
    .case-related,
    .case-detail-footer-cta {
      --case-body: 1.03125rem;
      --case-body-line: 1.66;
      --case-section: clamp(1.12rem, 5.2vw, 1.28rem);
      --case-hero-excerpt: 1.0625rem;
      --case-meta-label: 0.875rem;
      --case-meta-value: 0.96875rem;
      --case-sidebar-label: 0.875rem;
      --case-sidebar-value: 1rem;
      --case-card-title: 1.02rem;
    }

    .case-detail-hero__copy h1 {
      max-width: 100%;
      font-size: clamp(1.82rem, 6.15vw, 2.08rem);
      line-height: 1.1;
    }

    .case-detail-hero__copy > p {
      max-width: 34ch;
      font-size: var(--case-hero-excerpt);
      line-height: 1.58;
    }

    .case-detail-actions {
      display: grid;
      gap: 0.65rem;
      margin-top: 1.05rem;
    }

    .case-detail-actions :global(.um-btn) {
      width: 100%;
      min-width: 0;
      min-height: 48px;
      font-size: 0.98rem;
    }
  }

  @media (max-width: 720px) {
    .case-detail-hero {
      min-height: auto;
    }

    .case-detail-hero__content {
      padding-top: 30px;
      padding-bottom: 36px;
    }

    .case-detail-breadcrumb {
      display: none;
    }

    .case-detail-label::before {
      width: 28px;
      flex: 0 0 28px;
    }

    .case-detail-label {
      margin-bottom: 0.7rem;
      font-size: 0.95rem;
    }

    .case-detail-hero__copy h1 {
      font-size: clamp(1.82rem, 6.15vw, 2.08rem);
      line-height: 1.1;
    }

    .case-detail-actions {
      display: grid;
    }

    .case-detail-actions :global(.um-btn) {
      width: 100%;
    }

    .case-detail-dossier__media {
      aspect-ratio: 16 / 9;
      min-height: 0;
    }

    .case-detail-dossier__body {
      padding: 16px 0 0;
    }

    .case-detail-dossier__body > span {
      font-size: 0.86rem;
    }

    .case-detail-dossier__body > strong {
      font-size: clamp(1.12rem, 5vw, 1.3rem);
    }

    .case-detail-meta {
      grid-template-columns: 1fr;
      padding: 0.85rem;
    }

    .case-proof-strip__grid {
      grid-template-columns: 1fr;
      padding-block: 10px;
    }

    .case-proof-strip__grid div {
      padding: 12px 14px;
    }

    .case-detail-main {
      padding: 34px 0 48px;
    }
  }
</style>
