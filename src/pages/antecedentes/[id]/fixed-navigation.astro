// This is a temporary file to fix the navigation menu
// The content will be copied back to the original file

const fixedNavigation = `
  {/* Navigation Menu */}
  <nav class="fixed top-0 left-0 right-0 z-50 transition-all duration-500 backdrop-blur-md bg-black/30" id="mainNav">
    <div class="container mx-auto px-6 py-4">
      <div class="flex items-center justify-between">
        <a href="/" class="text-white text-2xl font-bold transform transition-transform hover:scale-105">
          <span class="text-red-500">Última</span> Milla
        </a>
        
        {/* Desktop Navigation */}
        <div class="hidden md:flex items-center space-x-8">
          <a href="/#servicios" class="nav-link">Servicios</a>
          <a href="/#proyectos" class="nav-link">Proyectos</a>
          <a href="/#nosotros" class="nav-link">Nosotros</a>
          <a href="/contacto" class="nav-button">
            <span class="relative z-10">Contacto</span>
            <span class="nav-button-hover"></span>
          </a>
        </div>
        
        {/* Mobile Menu Button */}
        <div class="md:hidden">
          <button class="text-white focus:outline-none hover:text-red-400 transition-colors" id="mobileMenuButton" aria-label="Menú móvil">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M4 6h16M4 12h16m-7 6h7" />
            </svg>
          </button>
        </div>
      </div>
    </div>
  </nav>
`;
