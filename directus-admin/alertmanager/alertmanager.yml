global:
  resolve_timeout: 5m
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'alertas@umbot.com.ar'
  smtp_auth_username: 'alertas@umbot.com.ar'
  smtp_auth_password: 'UmbotAlerts2025!'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email-notifications'

receivers:
- name: 'email-notifications'
  email_configs:
  - to: 'admin@umbot.com.ar'
    send_resolved: true 