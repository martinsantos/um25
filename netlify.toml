[build]
  command = "npm run build:preview"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect all routes to SSR function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/entry"
  status = 200
